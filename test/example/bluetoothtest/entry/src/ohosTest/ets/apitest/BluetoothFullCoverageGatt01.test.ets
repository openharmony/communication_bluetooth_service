/**
 * Auto-generated by skills/ohtest/bluetooth_full_coverage.py (do not edit by hand).
 * Suite: FullCoverage_gatt_client_01
 * Scenarios per API: n=normal, e=error, b=boundary, r=return, x=repeat, c=combo.
 */
import { describe, it, expect } from '@ohos/hypium';
import ble from '@ohos.bluetooth.ble';
import {
  fcRun, fcRunErr, fcRunRet, fcRunCombo,
  BADDR, BAD_MAC, EMPTY_MAC, SVC_UUID, CHAR_UUID,
  bleCharRef, bleDescRef, bleGattService, bleNotifyChar, blePhyValue, gattConnParam, hidSdpStub,
hidQosStub,
  sppOpt, legacySppOpt, bleAp, buf0, partnerDeviceAddr, partnerDeviceCap,
  partnerBusinessCap, gattServerResponse, sysBleStartOpt, sysBleStopOpt, sysBleFoundOpt
} from './ApitestFullCoverageShared';

export default function bluetoothFullCoverageGatt01() {
  describe('FullCoverage_gatt_client_01', () => {
    describe('gattc_connect', () => {
    it('gattc_connect_n', 0, () => {
      fcRun('gattc_connect_n', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.connect();expect(true).assertTrue();
      });
    });
    it('gattc_connect_e', 0, () => {
      fcRunErr('gattc_connect_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.connect();
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_connect_b', 0, () => {
      fcRun('gattc_connect_b', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.connect();expect(true).assertTrue();
      });
    });
    it('gattc_connect_r', 0, () => {
      fcRun('gattc_connect_r', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.connect();expect(true).assertTrue();
      });
    });
    it('gattc_connect_x', 0, () => {
      fcRun('gattc_connect_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.connect(); _g.connect();
        expect(true).assertTrue();
      });
    });
    it('gattc_connect_c', 0, () => {
      fcRunCombo('connect',[()=>{const g=ble.createGattClientDevice(BADDR);g.connect();g.close();}])
    });
    });
    describe('gattc_disconnect', () => {
    it('gattc_disconnect_n', 0, () => {
      fcRun('gattc_disconnect_n', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.disconnect();expect(true).assertTrue();
      });
    });
    it('gattc_disconnect_e', 0, () => {
      fcRunErr('gattc_disconnect_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.disconnect();
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_disconnect_b', 0, () => {
      fcRun('gattc_disconnect_b', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.disconnect();expect(true).assertTrue();
      });
    });
    it('gattc_disconnect_r', 0, () => {
      fcRun('gattc_disconnect_r', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.disconnect();expect(true).assertTrue();
      });
    });
    it('gattc_disconnect_x', 0, () => {
      fcRun('gattc_disconnect_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.disconnect(); _g.disconnect();
        expect(true).assertTrue();
      });
    });
    it('gattc_disconnect_c', 0, () => {
      fcRunCombo('disconnect',[()=>{const g=ble.createGattClientDevice(BADDR);g.disconnect();g.close();}])
    });
    });
    describe('gattc_close', () => {
    it('gattc_close_n', 0, () => {
      fcRun('gattc_close_n', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.close();expect(true).assertTrue();
      });
    });
    it('gattc_close_e', 0, () => {
      fcRunErr('gattc_close_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.close();
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_close_b', 0, () => {
      fcRun('gattc_close_b', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.close();expect(true).assertTrue();
      });
    });
    it('gattc_close_r', 0, () => {
      fcRun('gattc_close_r', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.close();expect(true).assertTrue();
      });
    });
    it('gattc_close_x', 0, () => {
      fcRun('gattc_close_x', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.close();_g.close();expect(true).assertTrue();
      });
    });
    it('gattc_close_c', 0, () => {
      fcRunCombo('close',[()=>{const g=ble.createGattClientDevice(BADDR);g.close();g.close();}])
    });
    });
    describe('gattc_getDeviceName', () => {
    it('gattc_getDeviceName_n', 0, () => {
      fcRun('gattc_getDeviceName_n', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getDeviceName();expect(true).assertTrue();
      });
    });
    it('gattc_getDeviceName_e', 0, () => {
      fcRunErr('gattc_getDeviceName_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.getDeviceName();
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_getDeviceName_b', 0, () => {
      fcRun('gattc_getDeviceName_b', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getDeviceName();expect(true).assertTrue();
      });
    });
    it('gattc_getDeviceName_r', 0, () => {
      fcRun('gattc_getDeviceName_r', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getDeviceName();expect(true).assertTrue();
      });
    });
    it('gattc_getDeviceName_x', 0, () => {
      fcRun('gattc_getDeviceName_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.getDeviceName(); _g.getDeviceName();
        expect(true).assertTrue();
      });
    });
    it('gattc_getDeviceName_c', 0, () => {
      fcRunCombo('getDeviceName',[()=>{const g=ble.createGattClientDevice(BADDR);g.getDeviceName();g.close();}])
    });
    });
    describe('gattc_getServices', () => {
    it('gattc_getServices_n', 0, () => {
      fcRun('gattc_getServices_n', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getServices();expect(true).assertTrue();
      });
    });
    it('gattc_getServices_e', 0, () => {
      fcRunErr('gattc_getServices_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.getServices();
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_getServices_b', 0, () => {
      fcRun('gattc_getServices_b', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getServices();expect(true).assertTrue();
      });
    });
    it('gattc_getServices_r', 0, () => {
      fcRun('gattc_getServices_r', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getServices();expect(true).assertTrue();
      });
    });
    it('gattc_getServices_x', 0, () => {
      fcRun('gattc_getServices_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.getServices(); _g.getServices();
        expect(true).assertTrue();
      });
    });
    it('gattc_getServices_c', 0, () => {
      fcRunCombo('getServices',[()=>{const g=ble.createGattClientDevice(BADDR);g.getServices();g.close();}])
    });
    });
    describe('gattc_readCharacteristicValue', () => {
    it('gattc_readCharacteristicValue_n', 0, () => {
      fcRun('gattc_readCharacteristicValue_n', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.readCharacteristicValue(bleCharRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_readCharacteristicValue_e', 0, () => {
      fcRunErr('gattc_readCharacteristicValue_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.readCharacteristicValue(bleCharRef());
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_readCharacteristicValue_b', 0, () => {
      fcRun('gattc_readCharacteristicValue_b', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.readCharacteristicValue(bleCharRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_readCharacteristicValue_r', 0, () => {
      fcRun('gattc_readCharacteristicValue_r', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.readCharacteristicValue(bleCharRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_readCharacteristicValue_x', 0, () => {
      fcRun('gattc_readCharacteristicValue_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.readCharacteristicValue(bleCharRef());
        _g.readCharacteristicValue(bleCharRef()); expect(true).assertTrue();
      });
    });
    it('gattc_readCharacteristicValue_c', 0, () => {
      fcRunCombo('readCharacteristicValue',[()=>{const g=ble.createGattClientDevice(BADDR);g.readCharacteristicValue(bleCharRef());g.close();}])
    });
    });
    describe('gattc_readDescriptorValue', () => {
    it('gattc_readDescriptorValue_n', 0, () => {
      fcRun('gattc_readDescriptorValue_n', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.readDescriptorValue(bleDescRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_readDescriptorValue_e', 0, () => {
      fcRunErr('gattc_readDescriptorValue_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.readDescriptorValue(bleDescRef());
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_readDescriptorValue_b', 0, () => {
      fcRun('gattc_readDescriptorValue_b', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.readDescriptorValue(bleDescRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_readDescriptorValue_r', 0, () => {
      fcRun('gattc_readDescriptorValue_r', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.readDescriptorValue(bleDescRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_readDescriptorValue_x', 0, () => {
      fcRun('gattc_readDescriptorValue_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.readDescriptorValue(bleDescRef());
        _g.readDescriptorValue(bleDescRef()); expect(true).assertTrue();
      });
    });
    it('gattc_readDescriptorValue_c', 0, () => {
      fcRunCombo('readDescriptorValue',[()=>{const g=ble.createGattClientDevice(BADDR);g.readDescriptorValue(bleDescRef());g.close();}])
    });
    });
    describe('gattc_writeCharacteristicValue', () => {
    it('gattc_writeCharacteristicValue_n', 0, () => {
      fcRun('gattc_writeCharacteristicValue_n', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.writeCharacteristicValue(bleCharRef(),ble.GattWriteType.WRITE);
        expect(true).assertTrue();
      });
    });
    it('gattc_writeCharacteristicValue_e', 0, () => {
      fcRunErr('gattc_writeCharacteristicValue_e', () => {
        const _g=ble.createGattClientDevice(BADDR);
        try{_g.writeCharacteristicValue(bleCharRef(),ble.GattWriteType.WRITE);
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_writeCharacteristicValue_b', 0, () => {
      fcRun('gattc_writeCharacteristicValue_b', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.writeCharacteristicValue(bleCharRef(),ble.GattWriteType.WRITE);
        expect(true).assertTrue();
      });
    });
    it('gattc_writeCharacteristicValue_r', 0, () => {
      fcRun('gattc_writeCharacteristicValue_r', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.writeCharacteristicValue(bleCharRef(),ble.GattWriteType.WRITE);
        expect(true).assertTrue();
      });
    });
    it('gattc_writeCharacteristicValue_x', 0, () => {
      fcRun('gattc_writeCharacteristicValue_x', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.writeCharacteristicValue(bleCharRef(),ble.GattWriteType.WRITE);
        _g.writeCharacteristicValue(bleCharRef(),ble.GattWriteType.WRITE);
        expect(true).assertTrue();
      });
    });
    it('gattc_writeCharacteristicValue_c', 0, () => {
      fcRunCombo('writeCharacteristicValue',[()=>{const g=ble.createGattClientDevice(BADDR);g.writeCharacteristicValue(bleCharRef(),ble.GattWriteType.WRITE);g.close();}])
    });
    });
    describe('gattc_writeCharacteristicValueWithContext', () => {
    it('gattc_writeCharacteristicValueWithContext_n', 0, () => {
      fcRun('gattc_writeCharacteristicValueWithContext_n', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.writeCharacteristicValueWithContext(bleCharRef(),ble.GattWriteType.WRITE);
        expect(true).assertTrue();
      });
    });
    it('gattc_writeCharacteristicValueWithContext_e', 0, () => {
      fcRunErr('gattc_writeCharacteristicValueWithContext_e', () => {
        const _g=ble.createGattClientDevice(BADDR);
        try{_g.writeCharacteristicValueWithContext(bleCharRef(),ble.GattWriteType.WRITE);
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_writeCharacteristicValueWithContext_b', 0, () => {
      fcRun('gattc_writeCharacteristicValueWithContext_b', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.writeCharacteristicValueWithContext(bleCharRef(),ble.GattWriteType.WRITE);
        expect(true).assertTrue();
      });
    });
    it('gattc_writeCharacteristicValueWithContext_r', 0, () => {
      fcRun('gattc_writeCharacteristicValueWithContext_r', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.writeCharacteristicValueWithContext(bleCharRef(),ble.GattWriteType.WRITE);
        expect(true).assertTrue();
      });
    });
    it('gattc_writeCharacteristicValueWithContext_x', 0, () => {
      fcRun('gattc_writeCharacteristicValueWithContext_x', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.writeCharacteristicValueWithContext(bleCharRef(),ble.GattWriteType.WRITE);
        _g.writeCharacteristicValueWithContext(bleCharRef(),ble.GattWriteType.WRITE);
        expect(true).assertTrue();
      });
    });
    it('gattc_writeCharacteristicValueWithContext_c', 0, () => {
      fcRunCombo('writeCharacteristicValueWithContext',[()=>{const g=ble.createGattClientDevice(BADDR);g.writeCharacteristicValueWithContext(bleCharRef(),ble.GattWriteType.WRITE);g.close();}])
    });
    });
    describe('gattc_writeDescriptorValue', () => {
    it('gattc_writeDescriptorValue_n', 0, () => {
      fcRun('gattc_writeDescriptorValue_n', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.writeDescriptorValue(bleDescRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_writeDescriptorValue_e', 0, () => {
      fcRunErr('gattc_writeDescriptorValue_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.writeDescriptorValue(bleDescRef());
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_writeDescriptorValue_b', 0, () => {
      fcRun('gattc_writeDescriptorValue_b', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.writeDescriptorValue(bleDescRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_writeDescriptorValue_r', 0, () => {
      fcRun('gattc_writeDescriptorValue_r', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.writeDescriptorValue(bleDescRef());
        expect(true).assertTrue();
      });
    });
    it('gattc_writeDescriptorValue_x', 0, () => {
      fcRun('gattc_writeDescriptorValue_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.writeDescriptorValue(bleDescRef());
        _g.writeDescriptorValue(bleDescRef()); expect(true).assertTrue();
      });
    });
    it('gattc_writeDescriptorValue_c', 0, () => {
      fcRunCombo('writeDescriptorValue',[()=>{const g=ble.createGattClientDevice(BADDR);g.writeDescriptorValue(bleDescRef());g.close();}])
    });
    });
    describe('gattc_getRssiValue', () => {
    it('gattc_getRssiValue_n', 0, () => {
      fcRun('gattc_getRssiValue_n', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getRssiValue();expect(true).assertTrue();
      });
    });
    it('gattc_getRssiValue_e', 0, () => {
      fcRunErr('gattc_getRssiValue_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.getRssiValue();
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_getRssiValue_b', 0, () => {
      fcRun('gattc_getRssiValue_b', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getRssiValue();expect(true).assertTrue();
      });
    });
    it('gattc_getRssiValue_r', 0, () => {
      fcRun('gattc_getRssiValue_r', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.getRssiValue();expect(true).assertTrue();
      });
    });
    it('gattc_getRssiValue_x', 0, () => {
      fcRun('gattc_getRssiValue_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.getRssiValue(); _g.getRssiValue();
        expect(true).assertTrue();
      });
    });
    it('gattc_getRssiValue_c', 0, () => {
      fcRunCombo('getRssiValue',[()=>{const g=ble.createGattClientDevice(BADDR);g.getRssiValue();g.close();}])
    });
    });
    describe('gattc_setBLEMtuSize', () => {
    it('gattc_setBLEMtuSize_n', 0, () => {
      fcRun('gattc_setBLEMtuSize_n', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.setBLEMtuSize(512);expect(true).assertTrue();
      });
    });
    it('gattc_setBLEMtuSize_e', 0, () => {
      fcRunErr('gattc_setBLEMtuSize_e', () => {
        const _g=ble.createGattClientDevice(BADDR); try{_g.setBLEMtuSize(512);
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_setBLEMtuSize_b', 0, () => {
      fcRun('gattc_setBLEMtuSize_b', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.setBLEMtuSize(512);expect(true).assertTrue();
      });
    });
    it('gattc_setBLEMtuSize_r', 0, () => {
      fcRun('gattc_setBLEMtuSize_r', () => {
        const _g=ble.createGattClientDevice(BADDR);_g.setBLEMtuSize(512);expect(true).assertTrue();
      });
    });
    it('gattc_setBLEMtuSize_x', 0, () => {
      fcRun('gattc_setBLEMtuSize_x', () => {
        const _g=ble.createGattClientDevice(BADDR); _g.setBLEMtuSize(512); _g.setBLEMtuSize(512);
        expect(true).assertTrue();
      });
    });
    it('gattc_setBLEMtuSize_c', 0, () => {
      fcRunCombo('setBLEMtuSize',[()=>{const g=ble.createGattClientDevice(BADDR);g.setBLEMtuSize(512);g.close();}])
    });
    });
    describe('gattc_setCharacteristicChangeNotification', () => {
    it('gattc_setCharacteristicChangeNotification_n', 0, () => {
      fcRun('gattc_setCharacteristicChangeNotification_n', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.setCharacteristicChangeNotification(bleCharRef(),true); expect(true).assertTrue();
      });
    });
    it('gattc_setCharacteristicChangeNotification_e', 0, () => {
      fcRunErr('gattc_setCharacteristicChangeNotification_e', () => {
        const _g=ble.createGattClientDevice(BADDR);
        try{_g.setCharacteristicChangeNotification(bleCharRef(),true);
        }catch(_e){expect(true).assertTrue(); }
      });
    });
    it('gattc_setCharacteristicChangeNotification_b', 0, () => {
      fcRun('gattc_setCharacteristicChangeNotification_b', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.setCharacteristicChangeNotification(bleCharRef(),true); expect(true).assertTrue();
      });
    });
    it('gattc_setCharacteristicChangeNotification_r', 0, () => {
      fcRun('gattc_setCharacteristicChangeNotification_r', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.setCharacteristicChangeNotification(bleCharRef(),true); expect(true).assertTrue();
      });
    });
    it('gattc_setCharacteristicChangeNotification_x', 0, () => {
      fcRun('gattc_setCharacteristicChangeNotification_x', () => {
        const _g=ble.createGattClientDevice(BADDR);
        _g.setCharacteristicChangeNotification(bleCharRef(),true);
        _g.setCharacteristicChangeNotification(bleCharRef(),true); expect(true).assertTrue();
      });
    });
    it('gattc_setCharacteristicChangeNotification_c', 0, () => {
      fcRunCombo('setCharacteristicChangeNotification',[()=>{const g=ble.createGattClientDevice(BADDR);g.setCharacteristicChangeNotification(bleCharRef(),true);g.close();}])
    });
    });
  });
}

