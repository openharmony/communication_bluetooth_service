/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Auto-generated by scripts/gen_conn_ble_socket_tests.py (do not edit by hand).
 * Part: connBleSocketMass01
 * connection / ble / socket: 6 scenarios per overload (n, e, b, e2, r, x).
 * Direct API calls only (no tI wrappers); LogUtil + real return/error assertions.
 */
import { describe, beforeEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import { BusinessError } from '@ohos.base';
import connection from '@ohos.bluetooth.connection';
import ble from '@ohos.bluetooth.ble';
import socket from '@ohos.bluetooth.socket';
import constant from '@ohos.bluetooth.constant';
import common from '@ohos.bluetooth.common';
import {
  buf0,
  sppOpt,
  bleAd,
  bleAs,
  bleAp,
  BADDR,
  assertPeerExpect,
  assertThrownErrIn,
  BT_DOC_ERR_CODES,
  ensureBtOn
} from './ConnBleSocketShared';

export default function connBleSocketMass01_p1() {
  describe('ConnBleSocket_mass_cn_00_29_p1', () => {
    beforeEach(() => {
    ensureBtOn();
    });
describe('cn_00_opt_profile', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_00_OPT_PROFILE_N
     * @tc.name cn_00_opt_profile_n
     * @tc.desc Verify Bluetooth API cn 00 opt profile normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_00_opt_profile_n', 0, () => {
      let errCode = 0;
      let value = -1;
      LogUtil.info('cn_00_opt_profile_n: invoke');
      try {
        value = connection.getProfileConnectionState();
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_00_opt_profile_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_00_opt_profile_n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      expect(errCode !== 0 || typeof value === 'number').assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_00_OPT_PROFILE_E
     * @tc.name cn_00_opt_profile_e
     * @tc.desc Verify Bluetooth API cn 00 opt profile error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_00_opt_profile_e', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_00_opt_profile_e: invoke');
      try {
        connection.getProfileConnectionState(constant.ProfileId.PROFILE_A2DP_SOURCE);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_00_opt_profile_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_00_opt_profile_e: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_00_OPT_PROFILE_B
     * @tc.name cn_00_opt_profile_b
     * @tc.desc Verify Bluetooth API cn 00 opt profile boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_00_opt_profile_b', 0, () => {
      let errCode = 0;
      let value = -1;
      LogUtil.info('cn_00_opt_profile_b: invoke');
      try {
        value = connection.getProfileConnectionState(constant.ProfileId.PROFILE_A2DP_SOURCE);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_00_opt_profile_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_00_opt_profile_b: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      expect(errCode !== 0 || typeof value === 'number').assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_00_OPT_PROFILE_E2
     * @tc.name cn_00_opt_profile_e2
     * @tc.desc Verify Bluetooth apitest case cn 00 opt profile e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_00_opt_profile_e2', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_00_opt_profile_e2: invoke');
      try {
        connection.getProfileConnectionState();
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_00_opt_profile_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_00_opt_profile_e2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_00_OPT_PROFILE_R
     * @tc.name cn_00_opt_profile_r
     * @tc.desc Verify Bluetooth API cn 00 opt profile return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_00_opt_profile_r', 0, () => {
      let errCode = 0;
      let value = -1;
      LogUtil.info('cn_00_opt_profile_r: invoke');
      try {
        value = connection.getProfileConnectionState();
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_00_opt_profile_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_00_opt_profile_r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      expect(errCode !== 0 || typeof value === 'number').assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_00_OPT_PROFILE_X
     * @tc.name cn_00_opt_profile_x
     * @tc.desc Verify Bluetooth API cn 00 opt profile repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_00_opt_profile_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_00_opt_profile_x: invoke');
      try {
        connection.getProfileConnectionState(constant.ProfileId.PROFILE_A2DP_SOURCE);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_00_opt_profile_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_00_opt_profile_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_01_pair_str_cb', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_01_PAIR_STR_CB_N
     * @tc.name cn_01_pair_str_cb_n
     * @tc.desc Verify Bluetooth API cn 01 pair str cb normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_01_pair_str_cb_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_01_pair_str_cb_n: invoke');
      try {
        connection.pairDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_01_pair_str_cb_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_01_pair_str_cb_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_01_PAIR_STR_CB_E
     * @tc.name cn_01_pair_str_cb_e
     * @tc.desc Verify Bluetooth API cn 01 pair str cb error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_01_pair_str_cb_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_01_pair_str_cb_e: invoke expect error');
      try {
        connection.pairDevice('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_01_pair_str_cb_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_01_pair_str_cb_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_01_PAIR_STR_CB_B
     * @tc.name cn_01_pair_str_cb_b
     * @tc.desc Verify Bluetooth API cn 01 pair str cb boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_01_pair_str_cb_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_01_pair_str_cb_b: invoke');
      try {
        connection.pairDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_01_pair_str_cb_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_01_pair_str_cb_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_01_PAIR_STR_CB_E2
     * @tc.name cn_01_pair_str_cb_e2
     * @tc.desc Verify Bluetooth apitest case cn 01 pair str cb e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_01_pair_str_cb_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_01_pair_str_cb_e2: invoke expect error');
      try {
        connection.pairDevice('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_01_pair_str_cb_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_01_pair_str_cb_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_01_PAIR_STR_CB_R
     * @tc.name cn_01_pair_str_cb_r
     * @tc.desc Verify Bluetooth API cn 01 pair str cb return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_01_pair_str_cb_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_01_pair_str_cb_r: invoke');
      try {
        connection.pairDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_01_pair_str_cb_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_01_pair_str_cb_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_01_PAIR_STR_CB_X
     * @tc.name cn_01_pair_str_cb_x
     * @tc.desc Verify Bluetooth API cn 01 pair str cb repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_01_pair_str_cb_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_01_pair_str_cb_x: invoke');
      try {
        connection.pairDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_01_pair_str_cb_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_01_pair_str_cb_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_02_pair_str_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_02_PAIR_STR_P_N
     * @tc.name cn_02_pair_str_p_n
     * @tc.desc Verify Bluetooth API cn 02 pair str p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_02_pair_str_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_02_pair_str_p_n: invoke');
      try {
        connection.pairDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_02_pair_str_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_02_pair_str_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_02_PAIR_STR_P_E
     * @tc.name cn_02_pair_str_p_e
     * @tc.desc Verify Bluetooth API cn 02 pair str p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_02_pair_str_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_02_pair_str_p_e: invoke expect error');
      try {
        connection.pairDevice('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_02_pair_str_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_02_pair_str_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_02_PAIR_STR_P_B
     * @tc.name cn_02_pair_str_p_b
     * @tc.desc Verify Bluetooth API cn 02 pair str p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_02_pair_str_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_02_pair_str_p_b: invoke');
      try {
        connection.pairDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_02_pair_str_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_02_pair_str_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_02_PAIR_STR_P_E2
     * @tc.name cn_02_pair_str_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 02 pair str p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_02_pair_str_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_02_pair_str_p_e2: invoke expect error');
      try {
        connection.pairDevice('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_02_pair_str_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_02_pair_str_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_02_PAIR_STR_P_R
     * @tc.name cn_02_pair_str_p_r
     * @tc.desc Verify Bluetooth API cn 02 pair str p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_02_pair_str_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_02_pair_str_p_r: invoke');
      try {
        connection.pairDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_02_pair_str_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_02_pair_str_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_02_PAIR_STR_P_X
     * @tc.name cn_02_pair_str_p_x
     * @tc.desc Verify Bluetooth API cn 02 pair str p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_02_pair_str_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_02_pair_str_p_x: invoke');
      try {
        connection.pairDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_02_pair_str_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_02_pair_str_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_03_pair_ba_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_03_PAIR_BA_P_N
     * @tc.name cn_03_pair_ba_p_n
     * @tc.desc Verify Bluetooth API cn 03 pair ba p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_03_pair_ba_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_03_pair_ba_p_n: invoke');
      try {
        connection.pairDevice({address:BADDR, addressType:common.BluetoothAddressType.VIRTUAL});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_03_pair_ba_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_03_pair_ba_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_03_PAIR_BA_P_E
     * @tc.name cn_03_pair_ba_p_e
     * @tc.desc Verify Bluetooth API cn 03 pair ba p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_03_pair_ba_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_03_pair_ba_p_e: invoke expect error');
      try {
        connection.pairDevice({address:'', addressType:common.BluetoothAddressType.VIRTUAL});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_03_pair_ba_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_03_pair_ba_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_03_PAIR_BA_P_B
     * @tc.name cn_03_pair_ba_p_b
     * @tc.desc Verify Bluetooth API cn 03 pair ba p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_03_pair_ba_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_03_pair_ba_p_b: invoke');
      try {
        connection.pairDevice({address:BADDR, addressType:common.BluetoothAddressType.REAL});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_03_pair_ba_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_03_pair_ba_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_03_PAIR_BA_P_E2
     * @tc.name cn_03_pair_ba_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 03 pair ba p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_03_pair_ba_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_03_pair_ba_p_e2: invoke expect error');
      try {
        connection.pairDevice({address:'', addressType:common.BluetoothAddressType.VIRTUAL});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_03_pair_ba_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_03_pair_ba_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_03_PAIR_BA_P_R
     * @tc.name cn_03_pair_ba_p_r
     * @tc.desc Verify Bluetooth API cn 03 pair ba p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_03_pair_ba_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_03_pair_ba_p_r: invoke');
      try {
        connection.pairDevice({address:BADDR, addressType:common.BluetoothAddressType.VIRTUAL});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_03_pair_ba_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_03_pair_ba_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_03_PAIR_BA_P_X
     * @tc.name cn_03_pair_ba_p_x
     * @tc.desc Verify Bluetooth API cn 03 pair ba p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_03_pair_ba_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_03_pair_ba_p_x: invoke');
      try {
        connection.pairDevice({address:BADDR, addressType:common.BluetoothAddressType.VIRTUAL});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_03_pair_ba_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_03_pair_ba_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_04_pair_cred_cb', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_04_PAIR_CRED_CB_N
     * @tc.name cn_04_pair_cred_cb_n
     * @tc.desc Verify Bluetooth API cn 04 pair cred cb normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_04_pair_cred_cb_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_04_pair_cred_cb_n: invoke');
      try {
        connection.pairCredibleDevice(BADDR, connection.BluetoothTransport.TRANSPORT_BR_EDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_04_pair_cred_cb_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_04_pair_cred_cb_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_04_PAIR_CRED_CB_E
     * @tc.name cn_04_pair_cred_cb_e
     * @tc.desc Verify Bluetooth API cn 04 pair cred cb error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_04_pair_cred_cb_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_04_pair_cred_cb_e: invoke expect error');
      try {
        connection.pairCredibleDevice('', connection.BluetoothTransport.TRANSPORT_BR_EDR, () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_04_pair_cred_cb_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_04_pair_cred_cb_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_04_PAIR_CRED_CB_B
     * @tc.name cn_04_pair_cred_cb_b
     * @tc.desc Verify Bluetooth API cn 04 pair cred cb boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_04_pair_cred_cb_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_04_pair_cred_cb_b: invoke');
      try {
        connection.pairCredibleDevice(BADDR, connection.BluetoothTransport.TRANSPORT_LE, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_04_pair_cred_cb_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_04_pair_cred_cb_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_04_PAIR_CRED_CB_E2
     * @tc.name cn_04_pair_cred_cb_e2
     * @tc.desc Verify Bluetooth apitest case cn 04 pair cred cb e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_04_pair_cred_cb_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_04_pair_cred_cb_e2: invoke expect error');
      try {
        connection.pairCredibleDevice('', connection.BluetoothTransport.TRANSPORT_BR_EDR, () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_04_pair_cred_cb_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_04_pair_cred_cb_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_04_PAIR_CRED_CB_R
     * @tc.name cn_04_pair_cred_cb_r
     * @tc.desc Verify Bluetooth API cn 04 pair cred cb return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_04_pair_cred_cb_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_04_pair_cred_cb_r: invoke');
      try {
        connection.pairCredibleDevice(BADDR, connection.BluetoothTransport.TRANSPORT_BR_EDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_04_pair_cred_cb_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_04_pair_cred_cb_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_04_PAIR_CRED_CB_X
     * @tc.name cn_04_pair_cred_cb_x
     * @tc.desc Verify Bluetooth API cn 04 pair cred cb repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_04_pair_cred_cb_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_04_pair_cred_cb_x: invoke');
      try {
        connection.pairCredibleDevice(BADDR, connection.BluetoothTransport.TRANSPORT_BR_EDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_04_pair_cred_cb_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_04_pair_cred_cb_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_05_pair_cred_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_05_PAIR_CRED_P_N
     * @tc.name cn_05_pair_cred_p_n
     * @tc.desc Verify Bluetooth API cn 05 pair cred p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_05_pair_cred_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_05_pair_cred_p_n: invoke');
      try {
        connection.pairCredibleDevice(BADDR, connection.BluetoothTransport.TRANSPORT_BR_EDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_05_pair_cred_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_05_pair_cred_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_05_PAIR_CRED_P_E
     * @tc.name cn_05_pair_cred_p_e
     * @tc.desc Verify Bluetooth API cn 05 pair cred p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_05_pair_cred_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_05_pair_cred_p_e: invoke expect error');
      try {
        connection.pairCredibleDevice('', connection.BluetoothTransport.TRANSPORT_BR_EDR);
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_05_pair_cred_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_05_pair_cred_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_05_PAIR_CRED_P_B
     * @tc.name cn_05_pair_cred_p_b
     * @tc.desc Verify Bluetooth API cn 05 pair cred p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_05_pair_cred_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_05_pair_cred_p_b: invoke');
      try {
        connection.pairCredibleDevice(BADDR, connection.BluetoothTransport.TRANSPORT_LE);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_05_pair_cred_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_05_pair_cred_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_05_PAIR_CRED_P_E2
     * @tc.name cn_05_pair_cred_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 05 pair cred p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_05_pair_cred_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_05_pair_cred_p_e2: invoke expect error');
      try {
        connection.pairCredibleDevice('', connection.BluetoothTransport.TRANSPORT_BR_EDR);
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_05_pair_cred_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_05_pair_cred_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_05_PAIR_CRED_P_R
     * @tc.name cn_05_pair_cred_p_r
     * @tc.desc Verify Bluetooth API cn 05 pair cred p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_05_pair_cred_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_05_pair_cred_p_r: invoke');
      try {
        connection.pairCredibleDevice(BADDR, connection.BluetoothTransport.TRANSPORT_BR_EDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_05_pair_cred_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_05_pair_cred_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_05_PAIR_CRED_P_X
     * @tc.name cn_05_pair_cred_p_x
     * @tc.desc Verify Bluetooth API cn 05 pair cred p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_05_pair_cred_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_05_pair_cred_p_x: invoke');
      try {
        connection.pairCredibleDevice(BADDR, connection.BluetoothTransport.TRANSPORT_BR_EDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_05_pair_cred_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_05_pair_cred_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_06_pair_oob_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_06_PAIR_OOB_P_N
     * @tc.name cn_06_pair_oob_p_n
     * @tc.desc Verify Bluetooth API cn 06 pair oob p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_06_pair_oob_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_06_pair_oob_p_n: invoke');
      try {
        connection.pairDeviceOutOfBand(connection.BluetoothTransport.TRANSPORT_BR_EDR, null, null);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_06_pair_oob_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_06_pair_oob_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_06_PAIR_OOB_P_E
     * @tc.name cn_06_pair_oob_p_e
     * @tc.desc Verify Bluetooth API cn 06 pair oob p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_06_pair_oob_p_e', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_06_pair_oob_p_e: invoke');
      try {
        connection.pairDeviceOutOfBand(connection.BluetoothTransport.TRANSPORT_LE, null, null);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_06_pair_oob_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_06_pair_oob_p_e: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_06_PAIR_OOB_P_B
     * @tc.name cn_06_pair_oob_p_b
     * @tc.desc Verify Bluetooth API cn 06 pair oob p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_06_pair_oob_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_06_pair_oob_p_b: invoke');
      try {
        connection.pairDeviceOutOfBand(connection.BluetoothTransport.TRANSPORT_BR_EDR, null, null);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_06_pair_oob_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_06_pair_oob_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_06_PAIR_OOB_P_E2
     * @tc.name cn_06_pair_oob_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 06 pair oob p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_06_pair_oob_p_e2', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_06_pair_oob_p_e2: invoke');
      try {
        connection.pairDeviceOutOfBand(connection.BluetoothTransport.TRANSPORT_BR_EDR, null, null);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_06_pair_oob_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_06_pair_oob_p_e2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_06_PAIR_OOB_P_R
     * @tc.name cn_06_pair_oob_p_r
     * @tc.desc Verify Bluetooth API cn 06 pair oob p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_06_pair_oob_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_06_pair_oob_p_r: invoke');
      try {
        connection.pairDeviceOutOfBand(connection.BluetoothTransport.TRANSPORT_BR_EDR, null, null);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_06_pair_oob_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_06_pair_oob_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_06_PAIR_OOB_P_X
     * @tc.name cn_06_pair_oob_p_x
     * @tc.desc Verify Bluetooth API cn 06 pair oob p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_06_pair_oob_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_06_pair_oob_p_x: invoke');
      try {
        connection.pairDeviceOutOfBand(connection.BluetoothTransport.TRANSPORT_LE, null, null);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_06_pair_oob_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_06_pair_oob_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_07_cancel_paired_cb', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_07_CANCEL_PAIRED_CB_N
     * @tc.name cn_07_cancel_paired_cb_n
     * @tc.desc Verify Bluetooth API cn 07 cancel paired cb normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_07_cancel_paired_cb_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_07_cancel_paired_cb_n: invoke');
      try {
        connection.cancelPairedDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_07_cancel_paired_cb_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_07_cancel_paired_cb_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_07_CANCEL_PAIRED_CB_E
     * @tc.name cn_07_cancel_paired_cb_e
     * @tc.desc Verify Bluetooth API cn 07 cancel paired cb error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_07_cancel_paired_cb_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_07_cancel_paired_cb_e: invoke expect error');
      try {
        connection.cancelPairedDevice('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_07_cancel_paired_cb_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_07_cancel_paired_cb_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_07_CANCEL_PAIRED_CB_B
     * @tc.name cn_07_cancel_paired_cb_b
     * @tc.desc Verify Bluetooth API cn 07 cancel paired cb boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_07_cancel_paired_cb_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_07_cancel_paired_cb_b: invoke');
      try {
        connection.cancelPairedDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_07_cancel_paired_cb_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_07_cancel_paired_cb_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_07_CANCEL_PAIRED_CB_E2
     * @tc.name cn_07_cancel_paired_cb_e2
     * @tc.desc Verify Bluetooth apitest case cn 07 cancel paired cb e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_07_cancel_paired_cb_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_07_cancel_paired_cb_e2: invoke expect error');
      try {
        connection.cancelPairedDevice('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_07_cancel_paired_cb_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_07_cancel_paired_cb_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_07_CANCEL_PAIRED_CB_R
     * @tc.name cn_07_cancel_paired_cb_r
     * @tc.desc Verify Bluetooth API cn 07 cancel paired cb return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_07_cancel_paired_cb_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_07_cancel_paired_cb_r: invoke');
      try {
        connection.cancelPairedDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_07_cancel_paired_cb_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_07_cancel_paired_cb_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_07_CANCEL_PAIRED_CB_X
     * @tc.name cn_07_cancel_paired_cb_x
     * @tc.desc Verify Bluetooth API cn 07 cancel paired cb repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_07_cancel_paired_cb_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_07_cancel_paired_cb_x: invoke');
      try {
        connection.cancelPairedDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_07_cancel_paired_cb_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_07_cancel_paired_cb_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_08_cancel_paired_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_08_CANCEL_PAIRED_P_N
     * @tc.name cn_08_cancel_paired_p_n
     * @tc.desc Verify Bluetooth API cn 08 cancel paired p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_08_cancel_paired_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_08_cancel_paired_p_n: invoke');
      try {
        connection.cancelPairedDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_08_cancel_paired_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_08_cancel_paired_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_08_CANCEL_PAIRED_P_E
     * @tc.name cn_08_cancel_paired_p_e
     * @tc.desc Verify Bluetooth API cn 08 cancel paired p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_08_cancel_paired_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_08_cancel_paired_p_e: invoke expect error');
      try {
        connection.cancelPairedDevice('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_08_cancel_paired_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_08_cancel_paired_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_08_CANCEL_PAIRED_P_B
     * @tc.name cn_08_cancel_paired_p_b
     * @tc.desc Verify Bluetooth API cn 08 cancel paired p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_08_cancel_paired_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_08_cancel_paired_p_b: invoke');
      try {
        connection.cancelPairedDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_08_cancel_paired_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_08_cancel_paired_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_08_CANCEL_PAIRED_P_E2
     * @tc.name cn_08_cancel_paired_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 08 cancel paired p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_08_cancel_paired_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_08_cancel_paired_p_e2: invoke expect error');
      try {
        connection.cancelPairedDevice('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_08_cancel_paired_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_08_cancel_paired_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_08_CANCEL_PAIRED_P_R
     * @tc.name cn_08_cancel_paired_p_r
     * @tc.desc Verify Bluetooth API cn 08 cancel paired p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_08_cancel_paired_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_08_cancel_paired_p_r: invoke');
      try {
        connection.cancelPairedDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_08_cancel_paired_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_08_cancel_paired_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_08_CANCEL_PAIRED_P_X
     * @tc.name cn_08_cancel_paired_p_x
     * @tc.desc Verify Bluetooth API cn 08 cancel paired p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_08_cancel_paired_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_08_cancel_paired_p_x: invoke');
      try {
        connection.cancelPairedDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_08_cancel_paired_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_08_cancel_paired_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_09_cancel_pair_cb', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_09_CANCEL_PAIR_CB_N
     * @tc.name cn_09_cancel_pair_cb_n
     * @tc.desc Verify Bluetooth API cn 09 cancel pair cb normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_09_cancel_pair_cb_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_09_cancel_pair_cb_n: invoke');
      try {
        connection.cancelPairingDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_09_cancel_pair_cb_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_09_cancel_pair_cb_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_09_CANCEL_PAIR_CB_E
     * @tc.name cn_09_cancel_pair_cb_e
     * @tc.desc Verify Bluetooth API cn 09 cancel pair cb error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_09_cancel_pair_cb_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_09_cancel_pair_cb_e: invoke expect error');
      try {
        connection.cancelPairingDevice('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_09_cancel_pair_cb_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_09_cancel_pair_cb_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_09_CANCEL_PAIR_CB_B
     * @tc.name cn_09_cancel_pair_cb_b
     * @tc.desc Verify Bluetooth API cn 09 cancel pair cb boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_09_cancel_pair_cb_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_09_cancel_pair_cb_b: invoke');
      try {
        connection.cancelPairingDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_09_cancel_pair_cb_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_09_cancel_pair_cb_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_09_CANCEL_PAIR_CB_E2
     * @tc.name cn_09_cancel_pair_cb_e2
     * @tc.desc Verify Bluetooth apitest case cn 09 cancel pair cb e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_09_cancel_pair_cb_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_09_cancel_pair_cb_e2: invoke expect error');
      try {
        connection.cancelPairingDevice('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_09_cancel_pair_cb_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_09_cancel_pair_cb_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_09_CANCEL_PAIR_CB_R
     * @tc.name cn_09_cancel_pair_cb_r
     * @tc.desc Verify Bluetooth API cn 09 cancel pair cb return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_09_cancel_pair_cb_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_09_cancel_pair_cb_r: invoke');
      try {
        connection.cancelPairingDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_09_cancel_pair_cb_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_09_cancel_pair_cb_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_09_CANCEL_PAIR_CB_X
     * @tc.name cn_09_cancel_pair_cb_x
     * @tc.desc Verify Bluetooth API cn 09 cancel pair cb repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_09_cancel_pair_cb_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_09_cancel_pair_cb_x: invoke');
      try {
        connection.cancelPairingDevice(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_09_cancel_pair_cb_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_09_cancel_pair_cb_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_10_cancel_pair_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_10_CANCEL_PAIR_P_N
     * @tc.name cn_10_cancel_pair_p_n
     * @tc.desc Verify Bluetooth API cn 10 cancel pair p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_10_cancel_pair_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_10_cancel_pair_p_n: invoke');
      try {
        connection.cancelPairingDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_10_cancel_pair_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_10_cancel_pair_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_10_CANCEL_PAIR_P_E
     * @tc.name cn_10_cancel_pair_p_e
     * @tc.desc Verify Bluetooth API cn 10 cancel pair p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_10_cancel_pair_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_10_cancel_pair_p_e: invoke expect error');
      try {
        connection.cancelPairingDevice('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_10_cancel_pair_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_10_cancel_pair_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_10_CANCEL_PAIR_P_B
     * @tc.name cn_10_cancel_pair_p_b
     * @tc.desc Verify Bluetooth API cn 10 cancel pair p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_10_cancel_pair_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_10_cancel_pair_p_b: invoke');
      try {
        connection.cancelPairingDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_10_cancel_pair_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_10_cancel_pair_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_10_CANCEL_PAIR_P_E2
     * @tc.name cn_10_cancel_pair_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 10 cancel pair p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_10_cancel_pair_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_10_cancel_pair_p_e2: invoke expect error');
      try {
        connection.cancelPairingDevice('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_10_cancel_pair_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_10_cancel_pair_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_10_CANCEL_PAIR_P_R
     * @tc.name cn_10_cancel_pair_p_r
     * @tc.desc Verify Bluetooth API cn 10 cancel pair p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_10_cancel_pair_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_10_cancel_pair_p_r: invoke');
      try {
        connection.cancelPairingDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_10_cancel_pair_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_10_cancel_pair_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_10_CANCEL_PAIR_P_X
     * @tc.name cn_10_cancel_pair_p_x
     * @tc.desc Verify Bluetooth API cn 10 cancel pair p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_10_cancel_pair_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_10_cancel_pair_p_x: invoke');
      try {
        connection.cancelPairingDevice(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_10_cancel_pair_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_10_cancel_pair_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
  });
}
