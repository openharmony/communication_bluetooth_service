/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Auto-generated by skills/ohtest/bluetooth_full_coverage.py (do not edit by hand).
 * Suite: FullCoverage_access_wear_system
 * Scenarios per API: n=normal, e=error, b=boundary, r=return, x=repeat, c=condition.
 * Direct API calls only (no fcRun wrappers); LogUtil + real return/error assertions.
 */
import { describe, beforeEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import { BusinessError } from '@ohos.base';
import access from '@ohos.bluetooth.access';
import wearDetection from '@ohos.bluetooth.wearDetection';
import ble from '@ohos.bluetooth.ble';
import {
  BADDR,
  BAD_MAC,
  EMPTY_MAC,
  SVC_UUID,
  CHAR_UUID,
  bleCharRef,
  bleDescRef,
  bleGattService,
  bleNotifyChar,
  blePhyValue,
  gattConnParam,
  hidSdpStub,
  hidQosStub,
  sppOpt,
  legacySppOpt,
  bleAp,
  bleAd,
  bleAs,
  buf0,
  partnerDeviceAddr,
  partnerDeviceCap,
  partnerBusinessCap,
  gattServerResponse,
  ctrlDeviceActionParams,
  trustedPairedDevices,
  sysBleStartOpt,
  sysBleStopOpt,
  sysBleFoundOpt,
  assertLocalOk,
  assertPeerExpect,
  assertThrownErrIn,
  BT_DOC_ERR_CODES,
  ensureBtOn,
  runPerf,
  runStress,
  waitCallback
} from './ApitestFullCoverageShared';

export default function bluetoothFullCoverageAccess01_p1() {
  describe('FullCoverage_access_wear_system_p1', () => {
    beforeEach(() => {
    ensureBtOn();
    });
describe('ac_enableBluetooth', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTH_N
     * @tc.name ac_enableBluetooth_n
     * @tc.desc Verify Bluetooth API ac enableBluetooth normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetooth_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        access.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTH_E
     * @tc.name ac_enableBluetooth_e
     * @tc.desc Verify Bluetooth API ac enableBluetooth error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetooth_e', 0, () => {
      let errCode = 0;
      LogUtil.info('e: invoke');
      try {
        access.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTH_B
     * @tc.name ac_enableBluetooth_b
     * @tc.desc Verify Bluetooth API ac enableBluetooth boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetooth_b', 0, () => {
      let errCode = 0;
      LogUtil.info('b: invoke');
      try {
        access.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTH_R
     * @tc.name ac_enableBluetooth_r
     * @tc.desc Verify Bluetooth API ac enableBluetooth return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetooth_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        access.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTH_X
     * @tc.name ac_enableBluetooth_x
     * @tc.desc Verify Bluetooth API ac enableBluetooth repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetooth_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        access.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertLocalOk(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        access.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTH_C
     * @tc.name ac_enableBluetooth_c
     * @tc.desc Verify Bluetooth API ac enableBluetooth condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetooth_c', 0, () => {
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        access.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTH_PERF
     * @tc.name ac_enableBluetooth_perf
     * @tc.desc Verify Bluetooth apitest case ac enableBluetooth perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetooth_perf', 0, () => {
      runPerf('ac_enableBluetooth_perf', 5000, () => {
        try {
          access.enableBluetooth();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTH_STRESS
     * @tc.name ac_enableBluetooth_stress
     * @tc.desc Verify Bluetooth apitest case ac enableBluetooth stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetooth_stress', 0, () => {
      runStress('ac_enableBluetooth_stress', 20, () => {
        try {
          access.enableBluetooth();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ac_disableBluetooth', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTH_N
     * @tc.name ac_disableBluetooth_n
     * @tc.desc Verify Bluetooth API ac disableBluetooth normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetooth_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        access.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTH_E
     * @tc.name ac_disableBluetooth_e
     * @tc.desc Verify Bluetooth API ac disableBluetooth error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetooth_e', 0, () => {
      let errCode = 0;
      LogUtil.info('e: invoke');
      try {
        access.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTH_B
     * @tc.name ac_disableBluetooth_b
     * @tc.desc Verify Bluetooth API ac disableBluetooth boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetooth_b', 0, () => {
      let errCode = 0;
      LogUtil.info('b: invoke');
      try {
        access.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTH_R
     * @tc.name ac_disableBluetooth_r
     * @tc.desc Verify Bluetooth API ac disableBluetooth return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetooth_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        access.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTH_X
     * @tc.name ac_disableBluetooth_x
     * @tc.desc Verify Bluetooth API ac disableBluetooth repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetooth_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        access.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertLocalOk(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        access.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTH_C
     * @tc.name ac_disableBluetooth_c
     * @tc.desc Verify Bluetooth API ac disableBluetooth condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetooth_c', 0, () => {
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        access.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertLocalOk(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTH_PERF
     * @tc.name ac_disableBluetooth_perf
     * @tc.desc Verify Bluetooth apitest case ac disableBluetooth perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetooth_perf', 0, () => {
      runPerf('ac_disableBluetooth_perf', 5000, () => {
        try {
          access.disableBluetooth();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTH_STRESS
     * @tc.name ac_disableBluetooth_stress
     * @tc.desc Verify Bluetooth apitest case ac disableBluetooth stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetooth_stress', 0, () => {
      runStress('ac_disableBluetooth_stress', 20, () => {
        try {
          access.disableBluetooth();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ac_enableBluetoothAsync', () => {
    it('ac_enableBluetoothAsync_n', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('n: invoke');
      try {
        value = access.enableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });
    it('ac_enableBluetoothAsync_e', 0, async (done: Function) => {
      let errCode = 0;
      LogUtil.info('e: invoke');
      try {
        access.enableBluetoothAsync();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: errCode=' + errCode);
      assertPeerExpect(errCode);
      waitCallback(done, 200);
    });
    it('ac_enableBluetoothAsync_b', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('b: invoke');
      try {
        value = access.enableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });
    it('ac_enableBluetoothAsync_r', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('r: invoke');
      try {
        value = access.enableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });
    it('ac_enableBluetoothAsync_x', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('x1: invoke');
      try {
        value = access.enableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      errCode = 0;
      value = null;
      LogUtil.info('x2: invoke');
      try {
        value = access.enableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });
    it('ac_enableBluetoothAsync_c', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('c: invoke');
      try {
        value = access.enableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTHASYNC_PERF
     * @tc.name ac_enableBluetoothAsync_perf
     * @tc.desc Verify Bluetooth apitest case ac enableBluetoothAsync perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetoothAsync_perf', 0, () => {
      runPerf('ac_enableBluetoothAsync_perf', 5000, () => {
        try {
          access.enableBluetoothAsync() as Object;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ENABLEBLUETOOTHASYNC_STRESS
     * @tc.name ac_enableBluetoothAsync_stress
     * @tc.desc Verify Bluetooth apitest case ac enableBluetoothAsync stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_enableBluetoothAsync_stress', 0, () => {
      runStress('ac_enableBluetoothAsync_stress', 20, () => {
        try {
          access.enableBluetoothAsync() as Object;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ac_disableBluetoothAsync', () => {
    it('ac_disableBluetoothAsync_n', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('n: invoke');
      try {
        value = access.disableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });
    it('ac_disableBluetoothAsync_e', 0, async (done: Function) => {
      let errCode = 0;
      LogUtil.info('e: invoke');
      try {
        access.disableBluetoothAsync();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: errCode=' + errCode);
      assertPeerExpect(errCode);
      waitCallback(done, 200);
    });
    it('ac_disableBluetoothAsync_b', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('b: invoke');
      try {
        value = access.disableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });
    it('ac_disableBluetoothAsync_r', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('r: invoke');
      try {
        value = access.disableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });
    it('ac_disableBluetoothAsync_x', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('x1: invoke');
      try {
        value = access.disableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      errCode = 0;
      value = null;
      LogUtil.info('x2: invoke');
      try {
        value = access.disableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });
    it('ac_disableBluetoothAsync_c', 0, async (done: Function) => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('c: invoke');
      try {
        value = access.disableBluetoothAsync() as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(value !== null).assertTrue();
        expect(typeof (value as ESObject).then).assertEqual('function');
      }
      waitCallback(done, 200);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTHASYNC_PERF
     * @tc.name ac_disableBluetoothAsync_perf
     * @tc.desc Verify Bluetooth apitest case ac disableBluetoothAsync perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetoothAsync_perf', 0, () => {
      runPerf('ac_disableBluetoothAsync_perf', 5000, () => {
        try {
          access.disableBluetoothAsync() as Object;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_DISABLEBLUETOOTHASYNC_STRESS
     * @tc.name ac_disableBluetoothAsync_stress
     * @tc.desc Verify Bluetooth apitest case ac disableBluetoothAsync stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_disableBluetoothAsync_stress', 0, () => {
      runStress('ac_disableBluetoothAsync_stress', 20, () => {
        try {
          access.disableBluetoothAsync() as Object;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ac_getState', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETSTATE_N
     * @tc.name ac_getState_n
     * @tc.desc Verify Bluetooth API ac getState normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getState_n', 0, () => {
      let errCode = 0;
      let state = -1;
      LogUtil.info('ac_getState_n: invoke');
      try {
        state = access.getState();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('ac_getState_n: ' + JSON.stringify(err));
      }
      LogUtil.info('ac_getState_n: errCode=' + errCode + ' state=' + state);
      expect(errCode).assertEqual(0);
      expect(typeof state).assertEqual('number');
      expect(state >= access.BluetoothState.STATE_OFF &&
        state <= access.BluetoothState.STATE_BLE_TURNING_OFF).assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETSTATE_E
     * @tc.name ac_getState_e
     * @tc.desc Verify Bluetooth API ac getState error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getState_e', 0, () => {
      let errCode = 0;
      LogUtil.info('ac_getState_e: invoke');
      try {
        access.getState();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('ac_getState_e: ' + JSON.stringify(err));
      }
      LogUtil.info('ac_getState_e: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETSTATE_B
     * @tc.name ac_getState_b
     * @tc.desc Verify Bluetooth API ac getState boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getState_b', 0, () => {
      let errCode = 0;
      let state = -1;
      LogUtil.info('ac_getState_b: invoke');
      try {
        state = access.getState();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('ac_getState_b: ' + JSON.stringify(err));
      }
      LogUtil.info('ac_getState_b: errCode=' + errCode + ' state=' + state);
      expect(errCode).assertEqual(0);
      expect(typeof state).assertEqual('number');
      expect(state >= access.BluetoothState.STATE_OFF &&
        state <= access.BluetoothState.STATE_BLE_TURNING_OFF).assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETSTATE_R
     * @tc.name ac_getState_r
     * @tc.desc Verify Bluetooth API ac getState return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getState_r', 0, () => {
      let errCode = 0;
      let state = -1;
      LogUtil.info('ac_getState_r: invoke');
      try {
        state = access.getState();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('ac_getState_r: ' + JSON.stringify(err));
      }
      LogUtil.info('ac_getState_r: errCode=' + errCode + ' state=' + state);
      expect(errCode).assertEqual(0);
      expect(typeof state).assertEqual('number');
      expect(state >= access.BluetoothState.STATE_OFF &&
        state <= access.BluetoothState.STATE_BLE_TURNING_OFF).assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETSTATE_X
     * @tc.name ac_getState_x
     * @tc.desc Verify Bluetooth API ac getState repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getState_x', 0, () => {
      let errCode = 0;
      let state = -1;
      LogUtil.info('ac_getState_x1: invoke');
      try {
        state = access.getState();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('ac_getState_x1: ' + JSON.stringify(err));
      }
      LogUtil.info('ac_getState_x1: errCode=' + errCode + ' state=' + state);
      expect(errCode).assertEqual(0);
      expect(typeof state).assertEqual('number');
      expect(state >= access.BluetoothState.STATE_OFF &&
        state <= access.BluetoothState.STATE_BLE_TURNING_OFF).assertTrue();
      errCode = 0;
      state = -1;
      LogUtil.info('ac_getState_x2: invoke');
      try {
        state = access.getState();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('ac_getState_x2: ' + JSON.stringify(err));
      }
      LogUtil.info('ac_getState_x2: errCode=' + errCode + ' state=' + state);
      expect(errCode).assertEqual(0);
      expect(typeof state).assertEqual('number');
      expect(state >= access.BluetoothState.STATE_OFF &&
        state <= access.BluetoothState.STATE_BLE_TURNING_OFF).assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETSTATE_C
     * @tc.name ac_getState_c
     * @tc.desc Verify Bluetooth API ac getState condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getState_c', 0, () => {
      let errCode = 0;
      let state = -1;
      LogUtil.info('ac_getState_c: invoke');
      try {
        state = access.getState();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('ac_getState_c: ' + JSON.stringify(err));
      }
      LogUtil.info('ac_getState_c: errCode=' + errCode + ' state=' + state);
      expect(errCode).assertEqual(0);
      expect(typeof state).assertEqual('number');
      expect(state >= access.BluetoothState.STATE_OFF &&
        state <= access.BluetoothState.STATE_BLE_TURNING_OFF).assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETSTATE_PERF
     * @tc.name ac_getState_perf
     * @tc.desc Verify Bluetooth apitest case ac getState perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getState_perf', 0, () => {
      runPerf('ac_getState_perf', 5000, () => {
        try {
          access.getState();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETSTATE_STRESS
     * @tc.name ac_getState_stress
     * @tc.desc Verify Bluetooth apitest case ac getState stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getState_stress', 0, () => {
      runStress('ac_getState_stress', 20, () => {
        try {
          access.getState();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ac_convertUuid', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_CONVERTUUID_N
     * @tc.name ac_convertUuid_n
     * @tc.desc Verify Bluetooth API ac convertUuid normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_convertUuid_n', 0, () => {
      let errCode = 0;
      let value = '';
      LogUtil.info('n: invoke');
      try {
        value = access.convertUuid('180A');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('string');
        expect(value.length >= 0).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_CONVERTUUID_E
     * @tc.name ac_convertUuid_e
     * @tc.desc Verify Bluetooth API ac convertUuid error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_convertUuid_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        access.convertUuid('');
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_CONVERTUUID_B
     * @tc.name ac_convertUuid_b
     * @tc.desc Verify Bluetooth API ac convertUuid boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_convertUuid_b', 0, () => {
      let errCode = 0;
      let value = '';
      LogUtil.info('b: invoke');
      try {
        value = access.convertUuid('0000180A-0000-1000-8000-00805F9B34FB');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('string');
        expect(value.length >= 0).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_CONVERTUUID_R
     * @tc.name ac_convertUuid_r
     * @tc.desc Verify Bluetooth API ac convertUuid return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_convertUuid_r', 0, () => {
      let errCode = 0;
      let value = '';
      LogUtil.info('r: invoke');
      try {
        value = access.convertUuid('180A');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('string');
        expect(value.length >= 0).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_CONVERTUUID_X
     * @tc.name ac_convertUuid_x
     * @tc.desc Verify Bluetooth API ac convertUuid repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_convertUuid_x', 0, () => {
      let errCode = 0;
      let value = '';
      LogUtil.info('x1: invoke');
      try {
        value = access.convertUuid('180A');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('string');
        expect(value.length >= 0).assertTrue();
      }
      errCode = 0;
      value = '';
      LogUtil.info('x2: invoke');
      try {
        value = access.convertUuid('180A');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('string');
        expect(value.length >= 0).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_CONVERTUUID_C
     * @tc.name ac_convertUuid_c
     * @tc.desc Verify Bluetooth API ac convertUuid condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_convertUuid_c', 0, () => {
      let errCode = 0;
      let value = '';
      LogUtil.info('c: invoke');
      try {
        value = access.convertUuid('180F');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('string');
        expect(value.length >= 0).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_CONVERTUUID_PERF
     * @tc.name ac_convertUuid_perf
     * @tc.desc Verify Bluetooth apitest case ac convertUuid perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_convertUuid_perf', 0, () => {
      runPerf('ac_convertUuid_perf', 5000, () => {
        try {
          access.convertUuid('180A');
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_CONVERTUUID_STRESS
     * @tc.name ac_convertUuid_stress
     * @tc.desc Verify Bluetooth apitest case ac convertUuid stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_convertUuid_stress', 0, () => {
      runStress('ac_convertUuid_stress', 20, () => {
        try {
          access.convertUuid('180A');
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ac_isValidRandomDeviceId', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ISVALIDRANDOMDEVICEID_N
     * @tc.name ac_isValidRandomDeviceId_n
     * @tc.desc Verify Bluetooth API ac isValidRandomDeviceId normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_isValidRandomDeviceId_n', 0, () => {
      let errCode = 0;
      let value = false;
      LogUtil.info('n: invoke');
      try {
        value = access.isValidRandomDeviceId(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ISVALIDRANDOMDEVICEID_E
     * @tc.name ac_isValidRandomDeviceId_e
     * @tc.desc Verify Bluetooth API ac isValidRandomDeviceId error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_isValidRandomDeviceId_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        access.isValidRandomDeviceId(EMPTY_MAC);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ISVALIDRANDOMDEVICEID_B
     * @tc.name ac_isValidRandomDeviceId_b
     * @tc.desc Verify Bluetooth API ac isValidRandomDeviceId boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_isValidRandomDeviceId_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        access.isValidRandomDeviceId(BAD_MAC);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ISVALIDRANDOMDEVICEID_R
     * @tc.name ac_isValidRandomDeviceId_r
     * @tc.desc Verify Bluetooth API ac isValidRandomDeviceId return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_isValidRandomDeviceId_r', 0, () => {
      let errCode = 0;
      let value = false;
      LogUtil.info('r: invoke');
      try {
        value = access.isValidRandomDeviceId(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ISVALIDRANDOMDEVICEID_X
     * @tc.name ac_isValidRandomDeviceId_x
     * @tc.desc Verify Bluetooth API ac isValidRandomDeviceId repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_isValidRandomDeviceId_x', 0, () => {
      let errCode = 0;
      let value = false;
      LogUtil.info('x1: invoke');
      try {
        value = access.isValidRandomDeviceId(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
      errCode = 0;
      value = false;
      LogUtil.info('x2: invoke');
      try {
        value = access.isValidRandomDeviceId(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ISVALIDRANDOMDEVICEID_C
     * @tc.name ac_isValidRandomDeviceId_c
     * @tc.desc Verify Bluetooth API ac isValidRandomDeviceId condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_isValidRandomDeviceId_c', 0, () => {
      let errCode = 0;
      let value = false;
      LogUtil.info('c: invoke');
      try {
        value = access.isValidRandomDeviceId(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ISVALIDRANDOMDEVICEID_PERF
     * @tc.name ac_isValidRandomDeviceId_perf
     * @tc.desc Verify Bluetooth apitest case ac isValidRandomDeviceId perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_isValidRandomDeviceId_perf', 0, () => {
      runPerf('ac_isValidRandomDeviceId_perf', 5000, () => {
        try {
          access.isValidRandomDeviceId(BADDR);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_ISVALIDRANDOMDEVICEID_STRESS
     * @tc.name ac_isValidRandomDeviceId_stress
     * @tc.desc Verify Bluetooth apitest case ac isValidRandomDeviceId stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_isValidRandomDeviceId_stress', 0, () => {
      runStress('ac_isValidRandomDeviceId_stress', 20, () => {
        try {
          access.isValidRandomDeviceId(BADDR);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ac_getPersistentDeviceIds', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETPERSISTENTDEVICEIDS_N
     * @tc.name ac_getPersistentDeviceIds_n
     * @tc.desc Verify Bluetooth API ac getPersistentDeviceIds normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getPersistentDeviceIds_n', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('n: invoke');
      try {
        value = access.getPersistentDeviceIds() as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETPERSISTENTDEVICEIDS_E
     * @tc.name ac_getPersistentDeviceIds_e
     * @tc.desc Verify Bluetooth API ac getPersistentDeviceIds error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getPersistentDeviceIds_e', 0, () => {
      let errCode = 0;
      LogUtil.info('e: invoke');
      try {
        access.getPersistentDeviceIds();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETPERSISTENTDEVICEIDS_B
     * @tc.name ac_getPersistentDeviceIds_b
     * @tc.desc Verify Bluetooth API ac getPersistentDeviceIds boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getPersistentDeviceIds_b', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('b: invoke');
      try {
        value = access.getPersistentDeviceIds() as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETPERSISTENTDEVICEIDS_R
     * @tc.name ac_getPersistentDeviceIds_r
     * @tc.desc Verify Bluetooth API ac getPersistentDeviceIds return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getPersistentDeviceIds_r', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('r: invoke');
      try {
        value = access.getPersistentDeviceIds() as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETPERSISTENTDEVICEIDS_X
     * @tc.name ac_getPersistentDeviceIds_x
     * @tc.desc Verify Bluetooth API ac getPersistentDeviceIds repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getPersistentDeviceIds_x', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('x1: invoke');
      try {
        value = access.getPersistentDeviceIds() as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
      errCode = 0;
      value = [];
      LogUtil.info('x2: invoke');
      try {
        value = access.getPersistentDeviceIds() as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETPERSISTENTDEVICEIDS_C
     * @tc.name ac_getPersistentDeviceIds_c
     * @tc.desc Verify Bluetooth API ac getPersistentDeviceIds condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getPersistentDeviceIds_c', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('c: invoke');
      try {
        value = access.getPersistentDeviceIds() as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertLocalOk(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETPERSISTENTDEVICEIDS_PERF
     * @tc.name ac_getPersistentDeviceIds_perf
     * @tc.desc Verify Bluetooth apitest case ac getPersistentDeviceIds perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getPersistentDeviceIds_perf', 0, () => {
      runPerf('ac_getPersistentDeviceIds_perf', 5000, () => {
        try {
          access.getPersistentDeviceIds() as Array < Object > ;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_AC_GETPERSISTENTDEVICEIDS_STRESS
     * @tc.name ac_getPersistentDeviceIds_stress
     * @tc.desc Verify Bluetooth apitest case ac getPersistentDeviceIds stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ac_getPersistentDeviceIds_stress', 0, () => {
      runStress('ac_getPersistentDeviceIds_stress', 20, () => {
        try {
          access.getPersistentDeviceIds() as Array < Object > ;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
  });
}
