/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Multi-module Scenario suite (1A). Generated by skills/ohtest/bluetooth_scenario_modules.py
 */
import { describe, beforeEach, afterEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import access from '@ohos.bluetooth.access';
import connection from '@ohos.bluetooth.connection';
import ble from '@ohos.bluetooth.ble';
import socket from '@ohos.bluetooth.socket';
import constant from '@ohos.bluetooth.constant';
import bluetooth from '@ohos.bluetooth';
import bluetoothManager from '@ohos.bluetoothManager';

import {
  assertLocalOk,
  assertPeerExpect,
  ensureBtOn,
  scInvoke,
  scEnable,
  scDisable,
  scGetState,
  scWaitStateOn,
  scAssertPreferOn,
  scAssertStateOn,
  scAssertStateOff,
  scAssertValidState,
  scAssertAfterDisable,
  scWaitStateOff,
  scAwaitStateOn,
  scAwaitStateOff,
  scDismissBtEnableDialog,
  scGetLocalName,
  scSetLocalName,
  scAssertNameRoundTrip,
  scAssertNameCannotSet,
  scAssertNameWhileBtOff,
  scAssertLocalFail,
  scAssertLocalFailOrBtOff,
  scRestoreName,
  scSafeStopBleScan,
  scSafeStopDiscovery,
  scSafeStopAdvertising,
  scSafeCloseGatt,
  scRepeat,
  scProfileIds,
  scScanOpt,
  scFilterEmpty,
  scFilterUuid,
  scAdvertiseSetting,
  scSppOpt,
  scAppendBleFinds,
  scAppendScannerFinds,
  scBleOnFind,
  scBleOffFind,
  scStartBleScan,
  scAmbientScanOpt,
  scClassicOnFind,
  scClassicOffFind,
  scStartClassicDiscovery,
  scPrepareAndStartClassicDiscovery,
  scAppendClassicFinds,
  SC_CLASSIC_FIND_WAIT_MS,
  scScannerOnFind,
  scScannerOffFind,
  scSafeStopScanner,
  scScannerStartScan,
  scScannerStopScan,
  scAssertBleScanHit,
  scAssertAmbientFindHit,
  scAssertBleScanMiss,
  scAssertBleCannotScan,
  SC_BLE_FIND_WAIT_MS,
  SC_BLE_MISS_UUID,
  bleAd,
  bleAs,
  bleAp,
  bleGattService,
  sppOpt,
  legacySppOpt,
  sysBleStartOpt,
  sysBleStopOpt,
  sysBleFoundOpt,
  partnerDeviceAddr,
  SC_SVC_UUID,
  SC_FAKE_MAC,
  BADDR,
  EMPTY_MAC,
  SPP_UUID
  } from './ApitestScenarioShared';

export default function bluetoothScenarioLegacy01() {
  describe('bluetoothScenarioLegacy01', () => {
    beforeEach(async () => {
      ensureBtOn();
      if (scGetState() !== access.BluetoothState.STATE_ON) {
        scEnable();
        await scDismissBtEnableDialog(2000);
        await new Promise<void>((resolve: () => void) => {
          setTimeout(resolve, 1500);
        });
      }
    });
    afterEach(async () => {
      scSafeStopBleScan();
      scSafeStopDiscovery();
      scSafeStopAdvertising();
      ensureBtOn();
      if (scGetState() !== access.BluetoothState.STATE_ON) {
        scEnable();
        await scDismissBtEnableDialog(2000);
        await new Promise<void>((resolve: () => void) => {
          setTimeout(resolve, 1500);
        });
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BT_ENABLE_GETSTATE
     * @tc.name leg_bt_enable_getState
     * @tc.desc L1: enable → bluetooth.getState
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enable_getState', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      let st = -1;
      const err = scInvoke(() => {
        st = bluetooth.getState() as number;
      });
      LogUtil.info('leg_bt state=' + st);
      assertLocalOk(err);
      scAssertStateOn(st);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BT_ENABLE_GETLOCALNAME
     * @tc.name leg_bt_enable_getLocalName
     * @tc.desc L1: enable → bluetooth.getLocalName
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enable_getLocalName', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      let prev = '';
      assertLocalOk(scInvoke(() => {
        prev = bluetooth.getLocalName();
      }));
      const want = 'ScLegGetName';
      assertLocalOk(scInvoke(() => {
        bluetooth.setLocalName(want);
      }));
      let n = '';
      assertLocalOk(scInvoke(() => {
        n = bluetooth.getLocalName();
      }));
      expect(n).assertEqual(want);
      if (prev.length > 0) {
        assertLocalOk(scInvoke(() => {
          bluetooth.setLocalName(prev);
        }));
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BT_ENABLE_GETPAIREDDEVICES
     * @tc.name leg_bt_enable_getPairedDevices
     * @tc.desc L1: enable → bluetooth.getPairedDevices
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enable_getPairedDevices', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      let list: string[] = [];
      const err = scInvoke(() => {
        list = bluetooth.getPairedDevices();
      });
      assertLocalOk(err);
      expect(list !== null).assertTrue();
      expect(list.length >= 0).assertTrue();
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BT_ENABLE_STARTBLESCAN
     * @tc.name leg_bt_enable_startBLEScan
     * @tc.desc L1: enable → bluetooth.startBLEScan
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enable_startBLEScan', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const err = scInvoke(() => {
        bluetooth.BLE.startBLEScan([]);
      });
      assertLocalOk(err);
      try {
        bluetooth.BLE.stopBLEScan();
      } catch (e) {
        LogUtil.error(JSON.stringify(e));
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BT_ENABLE_CREATEGATTSERVER
     * @tc.name leg_bt_enable_createGattServer
     * @tc.desc L1: enable → bluetooth.createGattServer
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enable_createGattServer', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const gs = bluetooth.BLE.createGattServer();
      expect(gs !== undefined).assertTrue();
      try {
        gs.close();
      } catch (e) {
        LogUtil.error(JSON.stringify(e));
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BT_ENABLE_SPPLISTEN
     * @tc.name leg_bt_enable_sppListen
     * @tc.desc L1: enable → bluetooth.sppListen
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enable_sppListen', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const err = scInvoke(() => {
        bluetooth.sppListen('leg', legacySppOpt(), () => {
        });
      });
      assertLocalOk(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BM_ENABLE_GETSTATE
     * @tc.name leg_bm_enable_getState
     * @tc.desc L1: enable → bluetoothManager.getState
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bm_enable_getState', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      let st = -1;
      const err = scInvoke(() => {
        st = bluetoothManager.getState() as number;
      });
      LogUtil.info('leg_bm state=' + st);
      assertLocalOk(err);
      scAssertStateOn(st);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BM_ENABLE_GETLOCALNAME
     * @tc.name leg_bm_enable_getLocalName
     * @tc.desc L1: enable → bluetoothManager.getLocalName
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bm_enable_getLocalName', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      let prev = '';
      assertLocalOk(scInvoke(() => {
        prev = bluetoothManager.getLocalName();
      }));
      const want = 'ScLegGetName';
      assertLocalOk(scInvoke(() => {
        bluetoothManager.setLocalName(want);
      }));
      let n = '';
      assertLocalOk(scInvoke(() => {
        n = bluetoothManager.getLocalName();
      }));
      expect(n).assertEqual(want);
      if (prev.length > 0) {
        assertLocalOk(scInvoke(() => {
          bluetoothManager.setLocalName(prev);
        }));
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BM_ENABLE_GETPAIREDDEVICES
     * @tc.name leg_bm_enable_getPairedDevices
     * @tc.desc L1: enable → bluetoothManager.getPairedDevices
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bm_enable_getPairedDevices', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      let list: string[] = [];
      const err = scInvoke(() => {
        list = bluetoothManager.getPairedDevices();
      });
      assertLocalOk(err);
      expect(list !== null).assertTrue();
      expect(list.length >= 0).assertTrue();
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BM_ENABLE_STARTBLESCAN
     * @tc.name leg_bm_enable_startBLEScan
     * @tc.desc L1: enable → bluetoothManager.startBLEScan
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bm_enable_startBLEScan', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const err = scInvoke(() => {
        bluetoothManager.BLE.startBLEScan([]);
      });
      assertLocalOk(err);
      try {
        bluetoothManager.BLE.stopBLEScan();
      } catch (e) {
        LogUtil.error(JSON.stringify(e));
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BM_ENABLE_CREATEGATTSERVER
     * @tc.name leg_bm_enable_createGattServer
     * @tc.desc L1: enable → bluetoothManager.createGattServer
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bm_enable_createGattServer', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const gs = bluetoothManager.BLE.createGattServer();
      expect(gs !== undefined).assertTrue();
      try {
        gs.close();
      } catch (e) {
        LogUtil.error(JSON.stringify(e));
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BM_ENABLE_SPPLISTEN
     * @tc.name leg_bm_enable_sppListen
     * @tc.desc L1: enable → bluetoothManager.sppListen
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bm_enable_sppListen', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const err = scInvoke(() => {
        bluetoothManager.sppListen('leg', legacySppOpt(), () => {
        });
      });
      assertLocalOk(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BT_ENABLEBLUETOOTH_GETSTATE
     * @tc.name leg_bt_enableBluetooth_getState
     * @tc.desc L1: bluetooth.enableBluetooth → getState == STATE_ON
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_getState', 0, () => {
      assertLocalOk(scInvoke(() => {
        bluetooth.enableBluetooth();
      }));
      scAssertStateOn(scWaitStateOn());
      let st = -1;
      assertLocalOk(scInvoke(() => {
        st = bluetooth.getState() as number;
      }));
      scAssertStateOn(st);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_BM_GETBTCONNECTIONSTATE
     * @tc.name leg_bm_getBtConnectionState
     * @tc.desc L1: enable → bluetoothManager.getBtConnectionState
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bm_getBtConnectionState', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      let st = -1;
      const err = scInvoke(() => {
        st = bluetoothManager.getBtConnectionState() as number;
      });
      LogUtil.info('btConnState=' + st + ' err=' + err);
      assertLocalOk(err);
      expect(st >= 0).assertTrue();
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_LEG_QUAD_BT_SCAN_GATT
     * @tc.name leg_quad_bt_scan_gatt
     * @tc.desc L2: enable → bluetooth.BLE.startBLEScan → createGattServer → stop → close
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_quad_bt_scan_gatt', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      assertLocalOk(scInvoke(() => {
        bluetooth.BLE.startBLEScan([]);
      }));
      const gs = bluetooth.BLE.createGattServer();
      assertLocalOk(scInvoke(() => {
        bluetooth.BLE.stopBLEScan();
      }));
      try {
        gs.close();
      } catch (e) {
        LogUtil.error(JSON.stringify(e));
      }
    });

  });
}
