/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Auto-generated by scripts/gen_conn_ble_socket_tests.py (do not edit by hand).
 * Part: connBleSocketMass02
 * connection / ble / socket: 6 scenarios per overload (n, e, b, e2, r, x).
 * Direct API calls only (no tI wrappers); LogUtil + real return/error assertions.
 */
import { describe, beforeEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import { BusinessError } from '@ohos.base';
import connection from '@ohos.bluetooth.connection';
import ble from '@ohos.bluetooth.ble';
import socket from '@ohos.bluetooth.socket';
import constant from '@ohos.bluetooth.constant';
import common from '@ohos.bluetooth.common';
import {
  buf0,
  sppOpt,
  bleAd,
  bleAs,
  bleAp,
  BADDR,
  assertPeerExpect,
  assertThrownErrIn,
  BT_DOC_ERR_CODES,
  ensureBtOn
} from './ConnBleSocketShared';

export default function connBleSocketMass02_p1() {
  describe('ConnBleSocket_mass_cn_30_58_p1', () => {
    beforeEach(() => {
    ensureBtOn();
    });
describe('cn_30_remote_uuids_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_30_REMOTE_UUIDS_P_N
     * @tc.name cn_30_remote_uuids_p_n
     * @tc.desc Verify Bluetooth API cn 30 remote uuids p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_30_remote_uuids_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_30_remote_uuids_p_n: invoke');
      try {
        connection.getRemoteProfileUuids(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_30_remote_uuids_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_30_remote_uuids_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_30_REMOTE_UUIDS_P_E
     * @tc.name cn_30_remote_uuids_p_e
     * @tc.desc Verify Bluetooth API cn 30 remote uuids p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_30_remote_uuids_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_30_remote_uuids_p_e: invoke expect error');
      try {
        connection.getRemoteProfileUuids('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_30_remote_uuids_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_30_remote_uuids_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_30_REMOTE_UUIDS_P_B
     * @tc.name cn_30_remote_uuids_p_b
     * @tc.desc Verify Bluetooth API cn 30 remote uuids p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_30_remote_uuids_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_30_remote_uuids_p_b: invoke');
      try {
        connection.getRemoteProfileUuids(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_30_remote_uuids_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_30_remote_uuids_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_30_REMOTE_UUIDS_P_E2
     * @tc.name cn_30_remote_uuids_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 30 remote uuids p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_30_remote_uuids_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_30_remote_uuids_p_e2: invoke expect error');
      try {
        connection.getRemoteProfileUuids('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_30_remote_uuids_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_30_remote_uuids_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_30_REMOTE_UUIDS_P_R
     * @tc.name cn_30_remote_uuids_p_r
     * @tc.desc Verify Bluetooth API cn 30 remote uuids p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_30_remote_uuids_p_r', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('cn_30_remote_uuids_p_r: invoke');
      try {
        connection.getRemoteProfileUuids(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_30_remote_uuids_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_30_remote_uuids_p_r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      expect(Array.isArray(value)).assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_30_REMOTE_UUIDS_P_X
     * @tc.name cn_30_remote_uuids_p_x
     * @tc.desc Verify Bluetooth API cn 30 remote uuids p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_30_remote_uuids_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_30_remote_uuids_p_x: invoke');
      try {
        connection.getRemoteProfileUuids(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_30_remote_uuids_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_30_remote_uuids_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_31_conn_allowed_cb', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_31_CONN_ALLOWED_CB_N
     * @tc.name cn_31_conn_allowed_cb_n
     * @tc.desc Verify Bluetooth API cn 31 conn allowed cb normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_31_conn_allowed_cb_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_31_conn_allowed_cb_n: invoke');
      try {
        connection.connectAllowedProfiles(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_31_conn_allowed_cb_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_31_conn_allowed_cb_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_31_CONN_ALLOWED_CB_E
     * @tc.name cn_31_conn_allowed_cb_e
     * @tc.desc Verify Bluetooth API cn 31 conn allowed cb error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_31_conn_allowed_cb_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_31_conn_allowed_cb_e: invoke expect error');
      try {
        connection.connectAllowedProfiles('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_31_conn_allowed_cb_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_31_conn_allowed_cb_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_31_CONN_ALLOWED_CB_B
     * @tc.name cn_31_conn_allowed_cb_b
     * @tc.desc Verify Bluetooth API cn 31 conn allowed cb boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_31_conn_allowed_cb_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_31_conn_allowed_cb_b: invoke');
      try {
        connection.connectAllowedProfiles(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_31_conn_allowed_cb_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_31_conn_allowed_cb_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_31_CONN_ALLOWED_CB_E2
     * @tc.name cn_31_conn_allowed_cb_e2
     * @tc.desc Verify Bluetooth apitest case cn 31 conn allowed cb e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_31_conn_allowed_cb_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_31_conn_allowed_cb_e2: invoke expect error');
      try {
        connection.connectAllowedProfiles('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_31_conn_allowed_cb_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_31_conn_allowed_cb_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_31_CONN_ALLOWED_CB_R
     * @tc.name cn_31_conn_allowed_cb_r
     * @tc.desc Verify Bluetooth API cn 31 conn allowed cb return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_31_conn_allowed_cb_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_31_conn_allowed_cb_r: invoke');
      try {
        connection.connectAllowedProfiles(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_31_conn_allowed_cb_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_31_conn_allowed_cb_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_31_CONN_ALLOWED_CB_X
     * @tc.name cn_31_conn_allowed_cb_x
     * @tc.desc Verify Bluetooth API cn 31 conn allowed cb repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_31_conn_allowed_cb_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_31_conn_allowed_cb_x: invoke');
      try {
        connection.connectAllowedProfiles(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_31_conn_allowed_cb_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_31_conn_allowed_cb_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_32_conn_allowed_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_32_CONN_ALLOWED_P_N
     * @tc.name cn_32_conn_allowed_p_n
     * @tc.desc Verify Bluetooth API cn 32 conn allowed p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_32_conn_allowed_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_32_conn_allowed_p_n: invoke');
      try {
        connection.connectAllowedProfiles(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_32_conn_allowed_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_32_conn_allowed_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_32_CONN_ALLOWED_P_E
     * @tc.name cn_32_conn_allowed_p_e
     * @tc.desc Verify Bluetooth API cn 32 conn allowed p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_32_conn_allowed_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_32_conn_allowed_p_e: invoke expect error');
      try {
        connection.connectAllowedProfiles('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_32_conn_allowed_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_32_conn_allowed_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_32_CONN_ALLOWED_P_B
     * @tc.name cn_32_conn_allowed_p_b
     * @tc.desc Verify Bluetooth API cn 32 conn allowed p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_32_conn_allowed_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_32_conn_allowed_p_b: invoke');
      try {
        connection.connectAllowedProfiles(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_32_conn_allowed_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_32_conn_allowed_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_32_CONN_ALLOWED_P_E2
     * @tc.name cn_32_conn_allowed_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 32 conn allowed p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_32_conn_allowed_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_32_conn_allowed_p_e2: invoke expect error');
      try {
        connection.connectAllowedProfiles('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_32_conn_allowed_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_32_conn_allowed_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_32_CONN_ALLOWED_P_R
     * @tc.name cn_32_conn_allowed_p_r
     * @tc.desc Verify Bluetooth API cn 32 conn allowed p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_32_conn_allowed_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_32_conn_allowed_p_r: invoke');
      try {
        connection.connectAllowedProfiles(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_32_conn_allowed_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_32_conn_allowed_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_32_CONN_ALLOWED_P_X
     * @tc.name cn_32_conn_allowed_p_x
     * @tc.desc Verify Bluetooth API cn 32 conn allowed p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_32_conn_allowed_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_32_conn_allowed_p_x: invoke');
      try {
        connection.connectAllowedProfiles(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_32_conn_allowed_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_32_conn_allowed_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_33_disc_allowed_cb', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_33_DISC_ALLOWED_CB_N
     * @tc.name cn_33_disc_allowed_cb_n
     * @tc.desc Verify Bluetooth API cn 33 disc allowed cb normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_33_disc_allowed_cb_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_33_disc_allowed_cb_n: invoke');
      try {
        connection.disconnectAllowedProfiles(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_33_disc_allowed_cb_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_33_disc_allowed_cb_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_33_DISC_ALLOWED_CB_E
     * @tc.name cn_33_disc_allowed_cb_e
     * @tc.desc Verify Bluetooth API cn 33 disc allowed cb error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_33_disc_allowed_cb_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_33_disc_allowed_cb_e: invoke expect error');
      try {
        connection.disconnectAllowedProfiles('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_33_disc_allowed_cb_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_33_disc_allowed_cb_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_33_DISC_ALLOWED_CB_B
     * @tc.name cn_33_disc_allowed_cb_b
     * @tc.desc Verify Bluetooth API cn 33 disc allowed cb boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_33_disc_allowed_cb_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_33_disc_allowed_cb_b: invoke');
      try {
        connection.disconnectAllowedProfiles(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_33_disc_allowed_cb_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_33_disc_allowed_cb_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_33_DISC_ALLOWED_CB_E2
     * @tc.name cn_33_disc_allowed_cb_e2
     * @tc.desc Verify Bluetooth apitest case cn 33 disc allowed cb e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_33_disc_allowed_cb_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_33_disc_allowed_cb_e2: invoke expect error');
      try {
        connection.disconnectAllowedProfiles('', () => {});
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_33_disc_allowed_cb_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_33_disc_allowed_cb_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_33_DISC_ALLOWED_CB_R
     * @tc.name cn_33_disc_allowed_cb_r
     * @tc.desc Verify Bluetooth API cn 33 disc allowed cb return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_33_disc_allowed_cb_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_33_disc_allowed_cb_r: invoke');
      try {
        connection.disconnectAllowedProfiles(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_33_disc_allowed_cb_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_33_disc_allowed_cb_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_33_DISC_ALLOWED_CB_X
     * @tc.name cn_33_disc_allowed_cb_x
     * @tc.desc Verify Bluetooth API cn 33 disc allowed cb repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_33_disc_allowed_cb_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_33_disc_allowed_cb_x: invoke');
      try {
        connection.disconnectAllowedProfiles(BADDR, () => {});
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_33_disc_allowed_cb_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_33_disc_allowed_cb_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_34_battery_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_34_BATTERY_P_N
     * @tc.name cn_34_battery_p_n
     * @tc.desc Verify Bluetooth API cn 34 battery p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_34_battery_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_34_battery_p_n: invoke');
      try {
        connection.getRemoteDeviceBatteryInfo(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_34_battery_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_34_battery_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_34_BATTERY_P_E
     * @tc.name cn_34_battery_p_e
     * @tc.desc Verify Bluetooth API cn 34 battery p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_34_battery_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_34_battery_p_e: invoke expect error');
      try {
        connection.getRemoteDeviceBatteryInfo('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_34_battery_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_34_battery_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_34_BATTERY_P_B
     * @tc.name cn_34_battery_p_b
     * @tc.desc Verify Bluetooth API cn 34 battery p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_34_battery_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_34_battery_p_b: invoke');
      try {
        connection.getRemoteDeviceBatteryInfo(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_34_battery_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_34_battery_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_34_BATTERY_P_E2
     * @tc.name cn_34_battery_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 34 battery p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_34_battery_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_34_battery_p_e2: invoke expect error');
      try {
        connection.getRemoteDeviceBatteryInfo('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_34_battery_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_34_battery_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_34_BATTERY_P_R
     * @tc.name cn_34_battery_p_r
     * @tc.desc Verify Bluetooth API cn 34 battery p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_34_battery_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_34_battery_p_r: invoke');
      try {
        connection.getRemoteDeviceBatteryInfo(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_34_battery_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_34_battery_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_34_BATTERY_P_X
     * @tc.name cn_34_battery_p_x
     * @tc.desc Verify Bluetooth API cn 34 battery p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_34_battery_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_34_battery_p_x: invoke');
      try {
        connection.getRemoteDeviceBatteryInfo(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_34_battery_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_34_battery_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_35_remote_product', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_35_REMOTE_PRODUCT_N
     * @tc.name cn_35_remote_product_n
     * @tc.desc Verify Bluetooth API cn 35 remote product normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_35_remote_product_n', 0, () => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('cn_35_remote_product_n: invoke');
      try {
        value = connection.getRemoteProductId(BADDR) as Object;
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_35_remote_product_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_35_remote_product_n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      expect(errCode !== 0 || (value !== null && value !== undefined)).assertTrue();
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_35_REMOTE_PRODUCT_E
     * @tc.name cn_35_remote_product_e
     * @tc.desc Verify Bluetooth API cn 35 remote product error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_35_remote_product_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_35_remote_product_e: invoke expect error');
      try {
        connection.getRemoteProductId('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_35_remote_product_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_35_remote_product_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_35_REMOTE_PRODUCT_B
     * @tc.name cn_35_remote_product_b
     * @tc.desc Verify Bluetooth API cn 35 remote product boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_35_remote_product_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_35_remote_product_b: invoke');
      try {
        connection.getRemoteProductId(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_35_remote_product_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_35_remote_product_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_35_REMOTE_PRODUCT_E2
     * @tc.name cn_35_remote_product_e2
     * @tc.desc Verify Bluetooth apitest case cn 35 remote product e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_35_remote_product_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_35_remote_product_e2: invoke expect error');
      try {
        connection.getRemoteProductId('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_35_remote_product_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_35_remote_product_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_35_REMOTE_PRODUCT_R
     * @tc.name cn_35_remote_product_r
     * @tc.desc Verify Bluetooth API cn 35 remote product return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_35_remote_product_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_35_remote_product_r: invoke');
      try {
        connection.getRemoteProductId(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_35_remote_product_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_35_remote_product_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_35_REMOTE_PRODUCT_X
     * @tc.name cn_35_remote_product_x
     * @tc.desc Verify Bluetooth API cn 35 remote product repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_35_remote_product_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_35_remote_product_x: invoke');
      try {
        connection.getRemoteProductId(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_35_remote_product_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_35_remote_product_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_36_disc_allowed_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_36_DISC_ALLOWED_P_N
     * @tc.name cn_36_disc_allowed_p_n
     * @tc.desc Verify Bluetooth API cn 36 disc allowed p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_36_disc_allowed_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_36_disc_allowed_p_n: invoke');
      try {
        connection.disconnectAllowedProfiles(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_36_disc_allowed_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_36_disc_allowed_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_36_DISC_ALLOWED_P_E
     * @tc.name cn_36_disc_allowed_p_e
     * @tc.desc Verify Bluetooth API cn 36 disc allowed p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_36_disc_allowed_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_36_disc_allowed_p_e: invoke expect error');
      try {
        connection.disconnectAllowedProfiles('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_36_disc_allowed_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_36_disc_allowed_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_36_DISC_ALLOWED_P_B
     * @tc.name cn_36_disc_allowed_p_b
     * @tc.desc Verify Bluetooth API cn 36 disc allowed p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_36_disc_allowed_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_36_disc_allowed_p_b: invoke');
      try {
        connection.disconnectAllowedProfiles(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_36_disc_allowed_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_36_disc_allowed_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_36_DISC_ALLOWED_P_E2
     * @tc.name cn_36_disc_allowed_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 36 disc allowed p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_36_disc_allowed_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_36_disc_allowed_p_e2: invoke expect error');
      try {
        connection.disconnectAllowedProfiles('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_36_disc_allowed_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_36_disc_allowed_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_36_DISC_ALLOWED_P_R
     * @tc.name cn_36_disc_allowed_p_r
     * @tc.desc Verify Bluetooth API cn 36 disc allowed p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_36_disc_allowed_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_36_disc_allowed_p_r: invoke');
      try {
        connection.disconnectAllowedProfiles(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_36_disc_allowed_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_36_disc_allowed_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_36_DISC_ALLOWED_P_X
     * @tc.name cn_36_disc_allowed_p_x
     * @tc.desc Verify Bluetooth API cn 36 disc allowed p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_36_disc_allowed_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_36_disc_allowed_p_x: invoke');
      try {
        connection.disconnectAllowedProfiles(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_36_disc_allowed_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_36_disc_allowed_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_37_set_remote_name_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_37_SET_REMOTE_NAME_P_N
     * @tc.name cn_37_set_remote_name_p_n
     * @tc.desc Verify Bluetooth API cn 37 set remote name p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_37_set_remote_name_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_37_set_remote_name_p_n: invoke');
      try {
        connection.setRemoteDeviceName(BADDR, 'n');
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_37_set_remote_name_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_37_set_remote_name_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_37_SET_REMOTE_NAME_P_E
     * @tc.name cn_37_set_remote_name_p_e
     * @tc.desc Verify Bluetooth API cn 37 set remote name p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_37_set_remote_name_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_37_set_remote_name_p_e: invoke expect error');
      try {
        connection.setRemoteDeviceName('', '');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_37_set_remote_name_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_37_set_remote_name_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_37_SET_REMOTE_NAME_P_B
     * @tc.name cn_37_set_remote_name_p_b
     * @tc.desc Verify Bluetooth API cn 37 set remote name p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_37_set_remote_name_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_37_set_remote_name_p_b: invoke');
      try {
        connection.setRemoteDeviceName(BADDR, 'nm');
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_37_set_remote_name_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_37_set_remote_name_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_37_SET_REMOTE_NAME_P_E2
     * @tc.name cn_37_set_remote_name_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 37 set remote name p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_37_set_remote_name_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_37_set_remote_name_p_e2: invoke expect error');
      try {
        connection.setRemoteDeviceName('', 'x');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_37_set_remote_name_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_37_set_remote_name_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_37_SET_REMOTE_NAME_P_R
     * @tc.name cn_37_set_remote_name_p_r
     * @tc.desc Verify Bluetooth API cn 37 set remote name p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_37_set_remote_name_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_37_set_remote_name_p_r: invoke');
      try {
        connection.setRemoteDeviceName(BADDR, 'n');
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_37_set_remote_name_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_37_set_remote_name_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_37_SET_REMOTE_NAME_P_X
     * @tc.name cn_37_set_remote_name_p_x
     * @tc.desc Verify Bluetooth API cn 37 set remote name p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_37_set_remote_name_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_37_set_remote_name_p_x: invoke');
      try {
        connection.setRemoteDeviceName(BADDR, 'n2');
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_37_set_remote_name_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_37_set_remote_name_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_38_set_remote_type_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_38_SET_REMOTE_TYPE_P_N
     * @tc.name cn_38_set_remote_type_p_n
     * @tc.desc Verify Bluetooth API cn 38 set remote type p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_38_set_remote_type_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_38_set_remote_type_p_n: invoke');
      try {
        connection.setRemoteDeviceType(BADDR, 0);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_38_set_remote_type_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_38_set_remote_type_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_38_SET_REMOTE_TYPE_P_E
     * @tc.name cn_38_set_remote_type_p_e
     * @tc.desc Verify Bluetooth API cn 38 set remote type p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_38_set_remote_type_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_38_set_remote_type_p_e: invoke expect error');
      try {
        connection.setRemoteDeviceType('', 0);
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_38_set_remote_type_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_38_set_remote_type_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_38_SET_REMOTE_TYPE_P_B
     * @tc.name cn_38_set_remote_type_p_b
     * @tc.desc Verify Bluetooth API cn 38 set remote type p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_38_set_remote_type_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_38_set_remote_type_p_b: invoke');
      try {
        connection.setRemoteDeviceType(BADDR, 1);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_38_set_remote_type_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_38_set_remote_type_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_38_SET_REMOTE_TYPE_P_E2
     * @tc.name cn_38_set_remote_type_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 38 set remote type p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_38_set_remote_type_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_38_set_remote_type_p_e2: invoke expect error');
      try {
        connection.setRemoteDeviceType('', 0);
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_38_set_remote_type_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_38_set_remote_type_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_38_SET_REMOTE_TYPE_P_R
     * @tc.name cn_38_set_remote_type_p_r
     * @tc.desc Verify Bluetooth API cn 38 set remote type p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_38_set_remote_type_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_38_set_remote_type_p_r: invoke');
      try {
        connection.setRemoteDeviceType(BADDR, 0);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_38_set_remote_type_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_38_set_remote_type_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_38_SET_REMOTE_TYPE_P_X
     * @tc.name cn_38_set_remote_type_p_x
     * @tc.desc Verify Bluetooth API cn 38 set remote type p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_38_set_remote_type_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_38_set_remote_type_p_x: invoke');
      try {
        connection.setRemoteDeviceType(BADDR, 0);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_38_set_remote_type_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_38_set_remote_type_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_39_get_remote_type_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_39_GET_REMOTE_TYPE_P_N
     * @tc.name cn_39_get_remote_type_p_n
     * @tc.desc Verify Bluetooth API cn 39 get remote type p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_39_get_remote_type_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_39_get_remote_type_p_n: invoke');
      try {
        connection.getRemoteDeviceType(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_39_get_remote_type_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_39_get_remote_type_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_39_GET_REMOTE_TYPE_P_E
     * @tc.name cn_39_get_remote_type_p_e
     * @tc.desc Verify Bluetooth API cn 39 get remote type p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_39_get_remote_type_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_39_get_remote_type_p_e: invoke expect error');
      try {
        connection.getRemoteDeviceType('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_39_get_remote_type_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_39_get_remote_type_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_39_GET_REMOTE_TYPE_P_B
     * @tc.name cn_39_get_remote_type_p_b
     * @tc.desc Verify Bluetooth API cn 39 get remote type p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_39_get_remote_type_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_39_get_remote_type_p_b: invoke');
      try {
        connection.getRemoteDeviceType(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_39_get_remote_type_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_39_get_remote_type_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_39_GET_REMOTE_TYPE_P_E2
     * @tc.name cn_39_get_remote_type_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 39 get remote type p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_39_get_remote_type_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_39_get_remote_type_p_e2: invoke expect error');
      try {
        connection.getRemoteDeviceType('');
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_39_get_remote_type_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_39_get_remote_type_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_39_GET_REMOTE_TYPE_P_R
     * @tc.name cn_39_get_remote_type_p_r
     * @tc.desc Verify Bluetooth API cn 39 get remote type p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_39_get_remote_type_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_39_get_remote_type_p_r: invoke');
      try {
        connection.getRemoteDeviceType(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_39_get_remote_type_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_39_get_remote_type_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_39_GET_REMOTE_TYPE_P_X
     * @tc.name cn_39_get_remote_type_p_x
     * @tc.desc Verify Bluetooth API cn 39 get remote type p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_39_get_remote_type_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_39_get_remote_type_p_x: invoke');
      try {
        connection.getRemoteDeviceType(BADDR);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_39_get_remote_type_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_39_get_remote_type_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
describe('cn_40_control_action_p', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_40_CONTROL_ACTION_P_N
     * @tc.name cn_40_control_action_p_n
     * @tc.desc Verify Bluetooth API cn 40 control action p normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_40_control_action_p_n', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_40_control_action_p_n: invoke');
      try {
        const p:connection.ControlDeviceActionParams={
      deviceId:BADDR, type:connection.ControlType.PLAY,
      typeValue:connection.ControlTypeValue.ENABLE,
      controlObject:connection.ControlObject.LEFT_EAR};
      connection.controlDeviceAction(p);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_40_control_action_p_n: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_40_control_action_p_n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_40_CONTROL_ACTION_P_E
     * @tc.name cn_40_control_action_p_e
     * @tc.desc Verify Bluetooth API cn 40 control action p error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_40_control_action_p_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_40_control_action_p_e: invoke expect error');
      try {
        try {const q:connection.ControlDeviceActionParams={
      deviceId:'', type:connection.ControlType.PLAY,
      typeValue:connection.ControlTypeValue.ENABLE,
      controlObject:connection.ControlObject.LEFT_EAR};
      connection.controlDeviceAction(q);
      } catch (_e) {
        LogUtil.error(JSON.stringify(_e));
      };
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_40_control_action_p_e: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_40_control_action_p_e: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_40_CONTROL_ACTION_P_B
     * @tc.name cn_40_control_action_p_b
     * @tc.desc Verify Bluetooth API cn 40 control action p boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_40_control_action_p_b', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_40_control_action_p_b: invoke');
      try {
        const r:connection.ControlDeviceActionParams={
      deviceId:BADDR, type:connection.ControlType.VIBRATE,
      typeValue:connection.ControlTypeValue.DISABLE,
      controlObject:connection.ControlObject.RIGHT_EAR};
      connection.controlDeviceAction(r);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_40_control_action_p_b: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_40_control_action_p_b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_40_CONTROL_ACTION_P_E2
     * @tc.name cn_40_control_action_p_e2
     * @tc.desc Verify Bluetooth apitest case cn 40 control action p e2
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_40_control_action_p_e2', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('cn_40_control_action_p_e2: invoke expect error');
      try {
        try {const s:connection.ControlDeviceActionParams={
      deviceId:'', type:connection.ControlType.PLAY,
      typeValue:connection.ControlTypeValue.ENABLE,
      controlObject:connection.ControlObject.LEFT_EAR};
      connection.controlDeviceAction(s);
      } catch (_e2) {
        LogUtil.error(JSON.stringify(_e2));
      };
      } catch (err) {
        threw = true;
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_40_control_action_p_e2: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_40_control_action_p_e2: threw=' + threw + ' errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_40_CONTROL_ACTION_P_R
     * @tc.name cn_40_control_action_p_r
     * @tc.desc Verify Bluetooth API cn 40 control action p return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_40_control_action_p_r', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_40_control_action_p_r: invoke');
      try {
        const t:connection.ControlDeviceActionParams={
      deviceId:BADDR, type:connection.ControlType.PLAY,
      typeValue:connection.ControlTypeValue.ENABLE,
      controlObject:connection.ControlObject.LEFT_EAR};
      connection.controlDeviceAction(t);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_40_control_action_p_r: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_40_control_action_p_r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_CN_40_CONTROL_ACTION_P_X
     * @tc.name cn_40_control_action_p_x
     * @tc.desc Verify Bluetooth API cn 40 control action p repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('cn_40_control_action_p_x', 0, () => {
      let errCode = 0;
      LogUtil.info('cn_40_control_action_p_x: invoke');
      try {
        const u:connection.ControlDeviceActionParams={
      deviceId:BADDR, type:connection.ControlType.PLAY,
      typeValue:connection.ControlTypeValue.ENABLE,
      controlObject:connection.ControlObject.LEFT_RIGHT_EAR};
      connection.controlDeviceAction(u);
      } catch (err) {
        const _c = (err as BusinessError).code;
        errCode = (_c === undefined || _c === null) ? 0 : Number(_c);
        if (isNaN(errCode)) {
          errCode = 0;
        }
        LogUtil.error('cn_40_control_action_p_x: ' + JSON.stringify(err));
      }
      LogUtil.info('cn_40_control_action_p_x: errCode=' + errCode);
      assertPeerExpect(errCode);
    });
    });
  });
}
