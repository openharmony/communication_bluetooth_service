/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Auto-generated by skills/ohtest/bluetooth_full_coverage.py (do not edit by hand).
 * Suite: FullCoverage_gatt_client_01
 * Scenarios per API: n=normal, e=error, b=boundary, r=return, x=repeat, c=condition.
 * Direct API calls only (no fcRun wrappers); LogUtil + real return/error assertions.
 */
import { describe, beforeEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import { BusinessError } from '@ohos.base';
import ble from '@ohos.bluetooth.ble';
import {
  BADDR,
  BAD_MAC,
  EMPTY_MAC,
  SVC_UUID,
  CHAR_UUID,
  bleCharRef,
  bleDescRef,
  bleGattService,
  bleNotifyChar,
  blePhyValue,
  gattConnParam,
  hidSdpStub,
  hidQosStub,
  sppOpt,
  legacySppOpt,
  bleAp,
  bleAd,
  bleAs,
  buf0,
  partnerDeviceAddr,
  partnerDeviceCap,
  partnerBusinessCap,
  gattServerResponse,
  ctrlDeviceActionParams,
  trustedPairedDevices,
  sysBleStartOpt,
  sysBleStopOpt,
  sysBleFoundOpt,
  assertLocalOk,
  assertPeerExpect,
  assertThrownErrIn,
  BT_DOC_ERR_CODES,
  ensureBtOn,
  runPerf,
  runStress,
  waitCallback
} from './ApitestFullCoverageShared';

export default function bluetoothFullCoverageGatt01_p1() {
  describe('FullCoverage_gatt_client_01_p1', () => {
    beforeEach(() => {
    ensureBtOn();
    });
describe('gattc_connect', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CONNECT_N
     * @tc.name gattc_connect_n
     * @tc.desc Verify Bluetooth API gattc connect normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_connect_n', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        gattClient.connect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CONNECT_E
     * @tc.name gattc_connect_e
     * @tc.desc Verify Bluetooth API gattc connect error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_connect_e', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        gattClient.connect();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CONNECT_B
     * @tc.name gattc_connect_b
     * @tc.desc Verify Bluetooth API gattc connect boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_connect_b', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        gattClient.connect();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CONNECT_R
     * @tc.name gattc_connect_r
     * @tc.desc Verify Bluetooth API gattc connect return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_connect_r', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        gattClient.connect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CONNECT_X
     * @tc.name gattc_connect_x
     * @tc.desc Verify Bluetooth API gattc connect repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_connect_x', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        gattClient.connect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        gattClient.connect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CONNECT_C
     * @tc.name gattc_connect_c
     * @tc.desc Verify Bluetooth API gattc connect condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_connect_c', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        gattClient.connect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CONNECT_PERF
     * @tc.name gattc_connect_perf
     * @tc.desc Verify Bluetooth apitest case gattc connect perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_connect_perf', 0, () => {
      runPerf('gattc_connect_perf', 5000, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.connect();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CONNECT_STRESS
     * @tc.name gattc_connect_stress
     * @tc.desc Verify Bluetooth apitest case gattc connect stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_connect_stress', 0, () => {
      runStress('gattc_connect_stress', 20, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.connect();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('gattc_disconnect', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_DISCONNECT_N
     * @tc.name gattc_disconnect_n
     * @tc.desc Verify Bluetooth API gattc disconnect normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_disconnect_n', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        gattClient.disconnect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_DISCONNECT_E
     * @tc.name gattc_disconnect_e
     * @tc.desc Verify Bluetooth API gattc disconnect error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_disconnect_e', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        gattClient.disconnect();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_DISCONNECT_B
     * @tc.name gattc_disconnect_b
     * @tc.desc Verify Bluetooth API gattc disconnect boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_disconnect_b', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        gattClient.disconnect();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_DISCONNECT_R
     * @tc.name gattc_disconnect_r
     * @tc.desc Verify Bluetooth API gattc disconnect return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_disconnect_r', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        gattClient.disconnect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_DISCONNECT_X
     * @tc.name gattc_disconnect_x
     * @tc.desc Verify Bluetooth API gattc disconnect repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_disconnect_x', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        gattClient.disconnect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        gattClient.disconnect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_DISCONNECT_C
     * @tc.name gattc_disconnect_c
     * @tc.desc Verify Bluetooth API gattc disconnect condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_disconnect_c', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        gattClient.disconnect();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_DISCONNECT_PERF
     * @tc.name gattc_disconnect_perf
     * @tc.desc Verify Bluetooth apitest case gattc disconnect perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_disconnect_perf', 0, () => {
      runPerf('gattc_disconnect_perf', 5000, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.disconnect();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_DISCONNECT_STRESS
     * @tc.name gattc_disconnect_stress
     * @tc.desc Verify Bluetooth apitest case gattc disconnect stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_disconnect_stress', 0, () => {
      runStress('gattc_disconnect_stress', 20, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.disconnect();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('gattc_close', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CLOSE_N
     * @tc.name gattc_close_n
     * @tc.desc Verify Bluetooth API gattc close normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_close_n', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        gattClient.close();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CLOSE_E
     * @tc.name gattc_close_e
     * @tc.desc Verify Bluetooth API gattc close error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_close_e', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        gattClient.close();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CLOSE_B
     * @tc.name gattc_close_b
     * @tc.desc Verify Bluetooth API gattc close boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_close_b', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        gattClient.close();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CLOSE_R
     * @tc.name gattc_close_r
     * @tc.desc Verify Bluetooth API gattc close return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_close_r', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        gattClient.close();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CLOSE_X
     * @tc.name gattc_close_x
     * @tc.desc Verify Bluetooth API gattc close repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_close_x', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        gattClient.close();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        gattClient.close();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CLOSE_C
     * @tc.name gattc_close_c
     * @tc.desc Verify Bluetooth API gattc close condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_close_c', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        gattClient.close();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CLOSE_PERF
     * @tc.name gattc_close_perf
     * @tc.desc Verify Bluetooth apitest case gattc close perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_close_perf', 0, () => {
      runPerf('gattc_close_perf', 5000, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.close();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_CLOSE_STRESS
     * @tc.name gattc_close_stress
     * @tc.desc Verify Bluetooth apitest case gattc close stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_close_stress', 0, () => {
      runStress('gattc_close_stress', 20, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.close();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('gattc_getDeviceName', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETDEVICENAME_N
     * @tc.name gattc_getDeviceName_n
     * @tc.desc Verify Bluetooth API gattc getDeviceName normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getDeviceName_n', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        gattClient.getDeviceName();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETDEVICENAME_E
     * @tc.name gattc_getDeviceName_e
     * @tc.desc Verify Bluetooth API gattc getDeviceName error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getDeviceName_e', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        gattClient.getDeviceName();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETDEVICENAME_B
     * @tc.name gattc_getDeviceName_b
     * @tc.desc Verify Bluetooth API gattc getDeviceName boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getDeviceName_b', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        gattClient.getDeviceName();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETDEVICENAME_R
     * @tc.name gattc_getDeviceName_r
     * @tc.desc Verify Bluetooth API gattc getDeviceName return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getDeviceName_r', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        gattClient.getDeviceName();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETDEVICENAME_X
     * @tc.name gattc_getDeviceName_x
     * @tc.desc Verify Bluetooth API gattc getDeviceName repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getDeviceName_x', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        gattClient.getDeviceName();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        gattClient.getDeviceName();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETDEVICENAME_C
     * @tc.name gattc_getDeviceName_c
     * @tc.desc Verify Bluetooth API gattc getDeviceName condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getDeviceName_c', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        gattClient.getDeviceName();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETDEVICENAME_PERF
     * @tc.name gattc_getDeviceName_perf
     * @tc.desc Verify Bluetooth apitest case gattc getDeviceName perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getDeviceName_perf', 0, () => {
      runPerf('gattc_getDeviceName_perf', 5000, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.getDeviceName();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETDEVICENAME_STRESS
     * @tc.name gattc_getDeviceName_stress
     * @tc.desc Verify Bluetooth apitest case gattc getDeviceName stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getDeviceName_stress', 0, () => {
      runStress('gattc_getDeviceName_stress', 20, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.getDeviceName();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('gattc_getServices', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETSERVICES_N
     * @tc.name gattc_getServices_n
     * @tc.desc Verify Bluetooth API gattc getServices normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getServices_n', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        gattClient.getServices();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETSERVICES_E
     * @tc.name gattc_getServices_e
     * @tc.desc Verify Bluetooth API gattc getServices error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getServices_e', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        gattClient.getServices();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETSERVICES_B
     * @tc.name gattc_getServices_b
     * @tc.desc Verify Bluetooth API gattc getServices boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getServices_b', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        gattClient.getServices();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETSERVICES_R
     * @tc.name gattc_getServices_r
     * @tc.desc Verify Bluetooth API gattc getServices return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getServices_r', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        gattClient.getServices();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETSERVICES_X
     * @tc.name gattc_getServices_x
     * @tc.desc Verify Bluetooth API gattc getServices repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getServices_x', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        gattClient.getServices();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        gattClient.getServices();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETSERVICES_C
     * @tc.name gattc_getServices_c
     * @tc.desc Verify Bluetooth API gattc getServices condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getServices_c', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        gattClient.getServices();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETSERVICES_PERF
     * @tc.name gattc_getServices_perf
     * @tc.desc Verify Bluetooth apitest case gattc getServices perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getServices_perf', 0, () => {
      runPerf('gattc_getServices_perf', 5000, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.getServices();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_GETSERVICES_STRESS
     * @tc.name gattc_getServices_stress
     * @tc.desc Verify Bluetooth apitest case gattc getServices stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_getServices_stress', 0, () => {
      runStress('gattc_getServices_stress', 20, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.getServices();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('gattc_readCharacteristicValue', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READCHARACTERISTICVALUE_N
     * @tc.name gattc_readCharacteristicValue_n
     * @tc.desc Verify Bluetooth API gattc readCharacteristicValue normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readCharacteristicValue_n', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        gattClient.readCharacteristicValue(bleCharRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READCHARACTERISTICVALUE_E
     * @tc.name gattc_readCharacteristicValue_e
     * @tc.desc Verify Bluetooth API gattc readCharacteristicValue error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readCharacteristicValue_e', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        gattClient.readCharacteristicValue(bleCharRef());
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READCHARACTERISTICVALUE_B
     * @tc.name gattc_readCharacteristicValue_b
     * @tc.desc Verify Bluetooth API gattc readCharacteristicValue boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readCharacteristicValue_b', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        gattClient.readCharacteristicValue(bleCharRef());
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READCHARACTERISTICVALUE_R
     * @tc.name gattc_readCharacteristicValue_r
     * @tc.desc Verify Bluetooth API gattc readCharacteristicValue return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readCharacteristicValue_r', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        gattClient.readCharacteristicValue(bleCharRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READCHARACTERISTICVALUE_X
     * @tc.name gattc_readCharacteristicValue_x
     * @tc.desc Verify Bluetooth API gattc readCharacteristicValue repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readCharacteristicValue_x', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        gattClient.readCharacteristicValue(bleCharRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        gattClient.readCharacteristicValue(bleCharRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READCHARACTERISTICVALUE_C
     * @tc.name gattc_readCharacteristicValue_c
     * @tc.desc Verify Bluetooth API gattc readCharacteristicValue condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readCharacteristicValue_c', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        gattClient.readCharacteristicValue(bleCharRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READCHARACTERISTICVALUE_PERF
     * @tc.name gattc_readCharacteristicValue_perf
     * @tc.desc Verify Bluetooth apitest case gattc readCharacteristicValue perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readCharacteristicValue_perf', 0, () => {
      runPerf('gattc_readCharacteristicValue_perf', 5000, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.readCharacteristicValue(bleCharRef());
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READCHARACTERISTICVALUE_STRESS
     * @tc.name gattc_readCharacteristicValue_stress
     * @tc.desc Verify Bluetooth apitest case gattc readCharacteristicValue stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readCharacteristicValue_stress', 0, () => {
      runStress('gattc_readCharacteristicValue_stress', 20, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.readCharacteristicValue(bleCharRef());
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('gattc_readDescriptorValue', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READDESCRIPTORVALUE_N
     * @tc.name gattc_readDescriptorValue_n
     * @tc.desc Verify Bluetooth API gattc readDescriptorValue normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readDescriptorValue_n', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        gattClient.readDescriptorValue(bleDescRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READDESCRIPTORVALUE_E
     * @tc.name gattc_readDescriptorValue_e
     * @tc.desc Verify Bluetooth API gattc readDescriptorValue error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readDescriptorValue_e', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        gattClient.readDescriptorValue(bleDescRef());
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READDESCRIPTORVALUE_B
     * @tc.name gattc_readDescriptorValue_b
     * @tc.desc Verify Bluetooth API gattc readDescriptorValue boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readDescriptorValue_b', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        gattClient.readDescriptorValue(bleDescRef());
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READDESCRIPTORVALUE_R
     * @tc.name gattc_readDescriptorValue_r
     * @tc.desc Verify Bluetooth API gattc readDescriptorValue return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readDescriptorValue_r', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        gattClient.readDescriptorValue(bleDescRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READDESCRIPTORVALUE_X
     * @tc.name gattc_readDescriptorValue_x
     * @tc.desc Verify Bluetooth API gattc readDescriptorValue repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readDescriptorValue_x', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        gattClient.readDescriptorValue(bleDescRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        gattClient.readDescriptorValue(bleDescRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READDESCRIPTORVALUE_C
     * @tc.name gattc_readDescriptorValue_c
     * @tc.desc Verify Bluetooth API gattc readDescriptorValue condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readDescriptorValue_c', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        gattClient.readDescriptorValue(bleDescRef());
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READDESCRIPTORVALUE_PERF
     * @tc.name gattc_readDescriptorValue_perf
     * @tc.desc Verify Bluetooth apitest case gattc readDescriptorValue perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readDescriptorValue_perf', 0, () => {
      runPerf('gattc_readDescriptorValue_perf', 5000, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.readDescriptorValue(bleDescRef());
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_READDESCRIPTORVALUE_STRESS
     * @tc.name gattc_readDescriptorValue_stress
     * @tc.desc Verify Bluetooth apitest case gattc readDescriptorValue stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_readDescriptorValue_stress', 0, () => {
      runStress('gattc_readDescriptorValue_stress', 20, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.readDescriptorValue(bleDescRef());
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('gattc_writeCharacteristicValue', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_WRITECHARACTERISTICVALUE_N
     * @tc.name gattc_writeCharacteristicValue_n
     * @tc.desc Verify Bluetooth API gattc writeCharacteristicValue normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_writeCharacteristicValue_n', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_WRITECHARACTERISTICVALUE_E
     * @tc.name gattc_writeCharacteristicValue_e
     * @tc.desc Verify Bluetooth API gattc writeCharacteristicValue error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_writeCharacteristicValue_e', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_WRITECHARACTERISTICVALUE_B
     * @tc.name gattc_writeCharacteristicValue_b
     * @tc.desc Verify Bluetooth API gattc writeCharacteristicValue boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_writeCharacteristicValue_b', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_WRITECHARACTERISTICVALUE_R
     * @tc.name gattc_writeCharacteristicValue_r
     * @tc.desc Verify Bluetooth API gattc writeCharacteristicValue return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_writeCharacteristicValue_r', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_WRITECHARACTERISTICVALUE_X
     * @tc.name gattc_writeCharacteristicValue_x
     * @tc.desc Verify Bluetooth API gattc writeCharacteristicValue repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_writeCharacteristicValue_x', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_WRITECHARACTERISTICVALUE_C
     * @tc.name gattc_writeCharacteristicValue_c
     * @tc.desc Verify Bluetooth API gattc writeCharacteristicValue condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_writeCharacteristicValue_c', 0, () => {
      const gattClient = ble.createGattClientDevice(BADDR);
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_WRITECHARACTERISTICVALUE_PERF
     * @tc.name gattc_writeCharacteristicValue_perf
     * @tc.desc Verify Bluetooth apitest case gattc writeCharacteristicValue perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_writeCharacteristicValue_perf', 0, () => {
      runPerf('gattc_writeCharacteristicValue_perf', 5000, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_GATTC_WRITECHARACTERISTICVALUE_STRESS
     * @tc.name gattc_writeCharacteristicValue_stress
     * @tc.desc Verify Bluetooth apitest case gattc writeCharacteristicValue stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('gattc_writeCharacteristicValue_stress', 0, () => {
      runStress('gattc_writeCharacteristicValue_stress', 20, () => {
        try {
          const gattClient = ble.createGattClientDevice(BADDR);
          gattClient.writeCharacteristicValue(bleCharRef(), ble.GattWriteType.WRITE);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
  });
}
