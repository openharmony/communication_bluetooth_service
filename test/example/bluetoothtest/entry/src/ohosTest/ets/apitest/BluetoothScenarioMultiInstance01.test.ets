/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Scenario / combinatorial flow suite (1A: single board, restore BT ON).
 * Generated by skills/ohtest/bluetooth_scenario_flow.py — prefer outcome asserts.
 */
import { describe, beforeEach, afterEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import access from '@ohos.bluetooth.access';
import connection from '@ohos.bluetooth.connection';
import ble from '@ohos.bluetooth.ble';
import constant from '@ohos.bluetooth.constant';
import {
  assertLocalOk,
  assertPeerExpect,
  ensureBtOn,
  scInvoke,
  scEnable,
  scDisable,
  scGetState,
  scWaitStateOn,
  scAssertPreferOn,
  scAssertStateOn,
  scAssertStateOff,
  scAssertValidState,
  scAssertAfterDisable,
  scWaitStateOff,
  scAwaitStateOn,
  scAwaitStateOff,
  scDismissBtEnableDialog,
  waitCallback,
  scGetLocalName,
  scSetLocalName,
  scAssertNameRoundTrip,
  scAssertNameCannotSet,
  scAssertNameWhileBtOff,
  scAssertLocalFail,
  scAssertLocalFailOrBtOff,
  scRestoreName,
  scSafeStopBleScan,
  scSafeStopDiscovery,
  scSafeStopAdvertising,
  scSafeCloseGatt,
  scRepeat,
  scProfileIds,
  scNameFixtures,
  scScanOpt,
  scFilterEmpty,
  scFilterName,
  scFilterUuid,
  scFilterMfg,
  scFilterCombo,
  scAppendBleFinds,
  scBleOnFind,
  scBleOffFind,
  scStartBleScan,
  scAmbientScanOpt,
  scClassicOnFind,
  scClassicOffFind,
  scStartClassicDiscovery,
  scPrepareAndStartClassicDiscovery,
  scAppendClassicFinds,
  SC_CLASSIC_FIND_WAIT_MS,
  scAppendScannerFinds,
  scScannerOnFind,
  scScannerOffFind,
  scSafeStopScanner,
  scScannerStartScan,
  scScannerStopScan,
  scAssertBleScanHit,
  scAssertAmbientFindHit,
  scAssertBleScanMiss,
  scAssertBleCannotScan,
  SC_BLE_FIND_WAIT_MS,
  SC_BLE_MISS_UUID,
  SC_SVC_UUID,
  SC_FAKE_MAC,
  SC_BAD_MAC,
  bleAs,
  bleAd
  } from './ApitestScenarioShared';

export default function bluetoothScenarioMultiInstance01() {
  describe('bluetoothScenarioMultiInstance01', () => {
    beforeEach(async () => {
      ensureBtOn();
      if (scGetState() !== access.BluetoothState.STATE_ON) {
        scEnable();
        await scDismissBtEnableDialog(2000);
        await new Promise<void>((resolve: () => void) => {
          setTimeout(resolve, 1500);
        });
      }
    });
    afterEach(async () => {
      scSafeStopBleScan();
      scSafeStopDiscovery();
      scSafeStopAdvertising();
      ensureBtOn();
      if (scGetState() !== access.BluetoothState.STATE_ON) {
        scEnable();
        await scDismissBtEnableDialog(2000);
        await new Promise<void>((resolve: () => void) => {
          setTimeout(resolve, 1500);
        });
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_GATTSERVER_X2
     * @tc.name multi_gattServer_x2
     * @tc.desc create 2 GattServers then close all
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_gattServer_x2', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const servers: Array<ble.GattServer> = [];
      for (let i = 0; i < 2; i++) {
        let gs: ble.GattServer | undefined = undefined;
        const err = scInvoke(() => {
          gs = ble.createGattServer();
        });
        LogUtil.info('multi gatt i=' + i + ' err=' + err);
        assertLocalOk(err);
        if (gs !== undefined) {
          servers.push(gs);
        }
      }
      expect(servers.length === 2).assertTrue();
      for (let j = 0; j < servers.length; j++) {
        scSafeCloseGatt(servers[j]);
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_GATTSERVER_X3
     * @tc.name multi_gattServer_x3
     * @tc.desc create 3 GattServers then close all
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_gattServer_x3', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const servers: Array<ble.GattServer> = [];
      for (let i = 0; i < 3; i++) {
        let gs: ble.GattServer | undefined = undefined;
        const err = scInvoke(() => {
          gs = ble.createGattServer();
        });
        LogUtil.info('multi gatt i=' + i + ' err=' + err);
        assertLocalOk(err);
        if (gs !== undefined) {
          servers.push(gs);
        }
      }
      expect(servers.length === 3).assertTrue();
      for (let j = 0; j < servers.length; j++) {
        scSafeCloseGatt(servers[j]);
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_GATTSERVER_X5
     * @tc.name multi_gattServer_x5
     * @tc.desc create 5 GattServers then close all
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_gattServer_x5', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const servers: Array<ble.GattServer> = [];
      for (let i = 0; i < 5; i++) {
        let gs: ble.GattServer | undefined = undefined;
        const err = scInvoke(() => {
          gs = ble.createGattServer();
        });
        LogUtil.info('multi gatt i=' + i + ' err=' + err);
        assertLocalOk(err);
        if (gs !== undefined) {
          servers.push(gs);
        }
      }
      expect(servers.length === 5).assertTrue();
      for (let j = 0; j < servers.length; j++) {
        scSafeCloseGatt(servers[j]);
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_BLESCANNER_X2
     * @tc.name multi_bleScanner_x2
     * @tc.desc create 2 BleScanners
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_bleScanner_x2', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const scanners: Array<ble.BleScanner> = [];
      for (let i = 0; i < 2; i++) {
        let scn: ble.BleScanner | undefined = undefined;
        const err = scInvoke(() => {
          scn = ble.createBleScanner();
        });
        LogUtil.info('multi scanner i=' + i + ' err=' + err);
        assertLocalOk(err);
        if (scn !== undefined) {
          scanners.push(scn);
        }
      }
      expect(scanners.length === 2).assertTrue();
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_BLESCANNER_X3
     * @tc.name multi_bleScanner_x3
     * @tc.desc create 3 BleScanners
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_bleScanner_x3', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const scanners: Array<ble.BleScanner> = [];
      for (let i = 0; i < 3; i++) {
        let scn: ble.BleScanner | undefined = undefined;
        const err = scInvoke(() => {
          scn = ble.createBleScanner();
        });
        LogUtil.info('multi scanner i=' + i + ' err=' + err);
        assertLocalOk(err);
        if (scn !== undefined) {
          scanners.push(scn);
        }
      }
      expect(scanners.length === 3).assertTrue();
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_BLESCANNER_X5
     * @tc.name multi_bleScanner_x5
     * @tc.desc create 5 BleScanners
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_bleScanner_x5', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const scanners: Array<ble.BleScanner> = [];
      for (let i = 0; i < 5; i++) {
        let scn: ble.BleScanner | undefined = undefined;
        const err = scInvoke(() => {
          scn = ble.createBleScanner();
        });
        LogUtil.info('multi scanner i=' + i + ' err=' + err);
        assertLocalOk(err);
        if (scn !== undefined) {
          scanners.push(scn);
        }
      }
      expect(scanners.length === 5).assertTrue();
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_DISCOVERY_START_TWICE
     * @tc.name multi_discovery_start_twice
     * @tc.desc startBluetoothDiscovery twice then stop
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_discovery_start_twice', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      assertLocalOk(scInvoke(() => {
        connection.startBluetoothDiscovery();
      }));
      const e2 = scInvoke(() => {
        connection.startBluetoothDiscovery();
      });
      LogUtil.info('multi discovery second err=' + e2);
      assertLocalOk(e2);
      assertLocalOk(scInvoke(() => {
        connection.stopBluetoothDiscovery();
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_DISCOVERY_LISTENERS_X3
     * @tc.name multi_discovery_listeners_x3
     * @tc.desc register 3 bluetoothDeviceFind listeners + discovery
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_discovery_listeners_x3', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const cbs: Array<(d: Array<string>) => void> = [];
      for (let i = 0; i < 3; i++) {
        const idx = i;
        const cb = (d: Array<string>): void => {
          LogUtil.info('find listener ' + idx + ' n=' + d.length);
        };
        cbs.push(cb);
        assertLocalOk(scInvoke(() => {
          connection.on('bluetoothDeviceFind', cb);
        }));
      }
      assertLocalOk(scInvoke(() => {
        connection.startBluetoothDiscovery();
      }));
      assertLocalOk(scInvoke(() => {
        connection.stopBluetoothDiscovery();
      }));
      for (let j = 0; j < cbs.length; j++) {
        assertLocalOk(scInvoke(() => {
          connection.off('bluetoothDeviceFind', cbs[j]);
        }));
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_STATECHANGE_LISTENERS_X3
     * @tc.name multi_stateChange_listeners_x3
     * @tc.desc register 3 stateChange listeners around enable
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_stateChange_listeners_x3', 0, () => {
      const cbs: Array<(s: access.BluetoothState) => void> = [];
      for (let i = 0; i < 3; i++) {
        const idx = i;
        const cb = (_s: access.BluetoothState): void => {
          LogUtil.info('state listener ' + idx);
        };
        cbs.push(cb);
        assertLocalOk(scInvoke(() => {
          access.on('stateChange', cb);
        }));
      }
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      for (let j = 0; j < cbs.length; j++) {
        assertLocalOk(scInvoke(() => {
          access.off('stateChange', cbs[j]);
        }));
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_GATT_AND_SCANNER_MIX
     * @tc.name multi_gatt_and_scanner_mix
     * @tc.desc create 2 GattServers + 2 BleScanners together
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_gatt_and_scanner_mix', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const g1 = ble.createGattServer();
      const g2 = ble.createGattServer();
      const s1 = ble.createBleScanner();
      const s2 = ble.createBleScanner();
      expect(g1 !== undefined).assertTrue();
      expect(g2 !== undefined).assertTrue();
      expect(s1 !== undefined).assertTrue();
      expect(s2 !== undefined).assertTrue();
      scSafeCloseGatt(g1);
      scSafeCloseGatt(g2);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_SCAN_THEN_DISCOVERY
     * @tc.name multi_scan_then_discovery
     * @tc.desc BLE scan + classic discovery concurrently then stop both
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_scan_then_discovery', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      assertLocalOk(scInvoke(() => {
        ble.startBLEScan(scFilterEmpty(), scScanOpt(0, ble.ScanDuty.SCAN_MODE_LOW_POWER));
      }));
      assertLocalOk(scInvoke(() => {
        connection.startBluetoothDiscovery();
      }));
      scAssertStateOn(scGetState());
      assertLocalOk(scInvoke(() => {
        connection.stopBluetoothDiscovery();
      }));
      assertLocalOk(scInvoke(() => {
        ble.stopBLEScan();
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_PROFILE_QUERY_ALL
     * @tc.name multi_profile_query_all
     * @tc.desc query all ProfileId connection states after enable
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_profile_query_all', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const ids = scProfileIds();
      for (let i = 0; i < ids.length; i++) {
        const err = scInvoke(() => {
          connection.getProfileConnectionState(ids[i] as constant.ProfileId);
        });
        LogUtil.info('multi profile i=' + i + ' err=' + err);
        assertLocalOk(err);
      }
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_MULTI_CREATE_CLOSE_GATT_LOOP
     * @tc.name multi_create_close_gatt_loop
     * @tc.desc create/close GattServer in loop x5
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('multi_create_close_gatt_loop', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      scRepeat('gatt_create_close', 5, () => {
        let gs: ble.GattServer | undefined = undefined;
        assertLocalOk(scInvoke(() => {
          gs = ble.createGattServer();
        }));
        if (gs !== undefined) {
          scSafeCloseGatt(gs);
        }
      });
    });

  });
}
