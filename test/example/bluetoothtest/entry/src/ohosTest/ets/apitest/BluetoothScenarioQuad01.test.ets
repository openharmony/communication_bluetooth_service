/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Scenario / combinatorial flow suite (1A: single board, restore BT ON).
 * Generated by skills/ohtest/bluetooth_scenario_flow.py — prefer outcome asserts.
 */
import { describe, beforeEach, afterEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import access from '@ohos.bluetooth.access';
import connection from '@ohos.bluetooth.connection';
import ble from '@ohos.bluetooth.ble';
import constant from '@ohos.bluetooth.constant';
import {
  assertLocalOk,
  assertPeerExpect,
  ensureBtOn,
  scInvoke,
  scEnable,
  scDisable,
  scGetState,
  scWaitStateOn,
  scAssertPreferOn,
  scAssertStateOn,
  scAssertStateOff,
  scAssertValidState,
  scAssertAfterDisable,
  scWaitStateOff,
  scAwaitStateOn,
  scAwaitStateOff,
  scDismissBtEnableDialog,
  waitCallback,
  scGetLocalName,
  scSetLocalName,
  scAssertNameRoundTrip,
  scAssertNameCannotSet,
  scAssertNameWhileBtOff,
  scAssertLocalFail,
  scAssertLocalFailOrBtOff,
  scRestoreName,
  scSafeStopBleScan,
  scSafeStopDiscovery,
  scSafeStopAdvertising,
  scSafeCloseGatt,
  scRepeat,
  scProfileIds,
  scNameFixtures,
  scScanOpt,
  scFilterEmpty,
  scFilterName,
  scFilterUuid,
  scFilterMfg,
  scFilterCombo,
  scAppendBleFinds,
  scBleOnFind,
  scBleOffFind,
  scStartBleScan,
  scAmbientScanOpt,
  scClassicOnFind,
  scClassicOffFind,
  scStartClassicDiscovery,
  scPrepareAndStartClassicDiscovery,
  scAppendClassicFinds,
  SC_CLASSIC_FIND_WAIT_MS,
  scAppendScannerFinds,
  scScannerOnFind,
  scScannerOffFind,
  scSafeStopScanner,
  scScannerStartScan,
  scScannerStopScan,
  scAssertBleScanHit,
  scAssertAmbientFindHit,
  scAssertBleScanMiss,
  scAssertBleCannotScan,
  SC_BLE_FIND_WAIT_MS,
  SC_BLE_MISS_UUID,
  SC_SVC_UUID,
  SC_FAKE_MAC,
  SC_BAD_MAC,
  bleAs,
  bleAd
  } from './ApitestScenarioShared';

export default function bluetoothScenarioQuad01() {
  describe('bluetoothScenarioQuad01', () => {
    beforeEach(async () => {
      ensureBtOn();
      if (scGetState() !== access.BluetoothState.STATE_ON) {
        scEnable();
        await scDismissBtEnableDialog(2000);
        await new Promise<void>((resolve: () => void) => {
          setTimeout(resolve, 1500);
        });
      }
    });
    afterEach(async () => {
      scSafeStopBleScan();
      scSafeStopDiscovery();
      scSafeStopAdvertising();
      ensureBtOn();
      if (scGetState() !== access.BluetoothState.STATE_ON) {
        scEnable();
        await scDismissBtEnableDialog(2000);
        await new Promise<void>((resolve: () => void) => {
          setTimeout(resolve, 1500);
        });
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_ON_GETSTATE_GETLOCALNAME
     * @tc.name quad_enable_on_getState_getLocalName
     * @tc.desc 4-way: on(stateChange) -> enable -> getState -> set/getLocalName
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_on_getState_getLocalName', 0, () => {
      const cb = (_s: access.BluetoothState): void => {
        LogUtil.info('quad stateChange');
      };
      assertLocalOk(scInvoke(() => {
        access.on('stateChange', cb);
      }));
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const prev = scGetLocalName();
      const want = 'ScQuadOnName';
      scAssertNameRoundTrip(scSetLocalName(want), want);
      scRestoreName(prev);
      assertLocalOk(scInvoke(() => {
        access.off('stateChange', cb);
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_STATE_NAME_PAIRED
     * @tc.name quad_enable_state_name_paired
     * @tc.desc 4-way: enable -> getState -> set/getLocalName -> getPairedDevices
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_state_name_paired', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const prev = scGetLocalName();
      const want = 'ScQuadName';
      scAssertNameRoundTrip(scSetLocalName(want), want);
      let list: string[] = [];
      assertLocalOk(scInvoke(() => {
        list = connection.getPairedDevices();
      }));
      expect(list !== null).assertTrue();
      scRestoreName(prev);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_STATE_PROFILE_SCANMODE
     * @tc.name quad_enable_state_profile_scanMode
     * @tc.desc 4-way: enable -> getState -> getProfileConnectionState -> getBluetoothScanMode
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_state_profile_scanMode', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      assertLocalOk(scInvoke(() => {
        connection.getProfileConnectionState();
      }));
      assertLocalOk(scInvoke(() => {
        connection.getBluetoothScanMode();
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_SETNAME_GETNAME_GETPAIRED_GETSTATE
     * @tc.name quad_enable_setName_getName_getPaired_getState
     * @tc.desc 4-way: enable -> setLocalName -> getLocalName -> getPairedDevices -> getState
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_setName_getName_getPaired_getState', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const prev = scGetLocalName();
      scAssertNameRoundTrip(scSetLocalName('ScQuad'), 'ScQuad');
      assertLocalOk(scInvoke(() => {
        connection.getPairedDevices();
      }));
      scAssertStateOn(scGetState());
      scRestoreName(prev);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_DISCOVERY_FIND_STOP_STATE
     * @tc.name quad_enable_discovery_find_stop_state
     * @tc.desc 4-way: enable -> on(find) -> startDiscovery -> stop -> getState
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_discovery_find_stop_state', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const cb = (_d: Array<string>): void => {
        LogUtil.info('quad find');
      };
      assertLocalOk(scInvoke(() => {
        connection.on('bluetoothDeviceFind', cb);
      }));
      assertLocalOk(scInvoke(() => {
        connection.startBluetoothDiscovery();
      }));
      assertLocalOk(scInvoke(() => {
        connection.stopBluetoothDiscovery();
      }));
      scAssertStateOn(scGetState());
      assertLocalOk(scInvoke(() => {
        connection.off('bluetoothDeviceFind', cb);
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_SCANOPT_START_STOP_STATE
     * @tc.name quad_enable_scanOpt_start_stop_state
     * @tc.desc 4-way: enable -> configure ScanOptions -> startBLEScan -> stop -> getState
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_scanOpt_start_stop_state', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const opt = scScanOpt(
      100,
      ble.ScanDuty.SCAN_MODE_BALANCED,
      ble.MatchMode.MATCH_MODE_AGGRESSIVE,
      ble.PhyType.PHY_LE_1M
      );
      assertLocalOk(scInvoke(() => {
        ble.startBLEScan(scFilterEmpty(), opt);
      }));
      assertLocalOk(scInvoke(() => {
        ble.stopBLEScan();
      }));
      scAssertStateOn(scGetState());
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_GATT_SCANNER_STATE_NAME
     * @tc.name quad_enable_gatt_scanner_state_name
     * @tc.desc 4-way: enable -> createGattServer -> createBleScanner -> getState -> getLocalName
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_gatt_scanner_state_name', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      let gs: ble.GattServer | undefined = undefined;
      let scn: ble.BleScanner | undefined = undefined;
      assertLocalOk(scInvoke(() => {
        gs = ble.createGattServer();
      }));
      assertLocalOk(scInvoke(() => {
        scn = ble.createBleScanner();
      }));
      scAssertStateOn(scGetState());
      const prev = scGetLocalName();
      const want = 'ScGattScnName';
      scAssertNameRoundTrip(scSetLocalName(want), want);
      scRestoreName(prev);
      if (gs !== undefined) {
        scSafeCloseGatt(gs);
      }
      expect(scn !== undefined).assertTrue();
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_OFF_ON_OFF_ON
     * @tc.name quad_off_on_off_on
     * @tc.desc 4-way power: disable -> enable -> disable -> enable
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_off_on_off_on', 0, async () => {
      assertLocalOk(scDisable());
      assertLocalOk(scEnable());
      scAssertStateOn(await scAwaitStateOn(2500));
      assertLocalOk(scDisable());
      assertLocalOk(scEnable());
      scAssertStateOn(await scAwaitStateOn(2500));
      }, 45000);


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ON_OFF_ON_GETNAME
     * @tc.name quad_on_off_on_getName
     * @tc.desc 4-way power: enable -> disable -> enable -> getLocalName
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_on_off_on_getName', 0, async () => {
      assertLocalOk(scEnable());
      scAssertStateOn(await scAwaitStateOn(2500));
      assertLocalOk(scDisable());
      scAssertStateOff(await scAwaitStateOff(2500));
      assertLocalOk(scEnable());
      scAssertStateOn(await scAwaitStateOn(2500));
      const prev = scGetLocalName();
      const want = 'ScPwrName';
      scAssertNameRoundTrip(scSetLocalName(want), want);
      scRestoreName(prev);
      }, 45000);


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_SETSCAN_GETSCAN_PAIRED_STATE
     * @tc.name quad_enable_setScan_getScan_paired_state
     * @tc.desc 4-way: enable -> setScanMode -> getScanMode -> getPairedDevices -> getState
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_setScan_getScan_paired_state', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      assertLocalOk(scInvoke(() => {
        connection.setBluetoothScanMode(
        connection.ScanMode.SCAN_MODE_GENERAL_DISCOVERABLE, 30);
      }));
      assertLocalOk(scInvoke(() => {
        connection.getBluetoothScanMode();
      }));
      assertLocalOk(scInvoke(() => {
        connection.getPairedDevices();
      }));
      scAssertStateOn(scGetState());
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_FILTER_SCAN_STOP_PAIRED
     * @tc.name quad_enable_filter_scan_stop_paired
     * @tc.desc 4-way: enable -> filter+options scan -> stop -> getPairedDevices
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_filter_scan_stop_paired', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      const opt = scScanOpt(0, ble.ScanDuty.SCAN_MODE_LOW_LATENCY);
      assertLocalOk(scInvoke(() => {
        ble.startBLEScan(scFilterUuid(SC_SVC_UUID), opt);
      }));
      assertLocalOk(scInvoke(() => {
        ble.stopBLEScan();
      }));
      assertLocalOk(scInvoke(() => {
        connection.getPairedDevices();
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_CHAIN_LOCAL_QUERIES
     * @tc.name quad_chain_local_queries
     * @tc.desc 4-way local query chain after enable
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_chain_local_queries', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      scAssertStateOn(scGetState());
      const prev = scGetLocalName();
      const want = 'ScQLocal';
      scAssertNameRoundTrip(scSetLocalName(want), want);
      let list: string[] = [];
      assertLocalOk(scInvoke(() => {
        list = connection.getPairedDevices();
      }));
      expect(list !== null).assertTrue();
      let mode = -1;
      assertLocalOk(scInvoke(() => {
        mode = connection.getBluetoothScanMode() as number;
      }));
      expect(mode >= connection.ScanMode.SCAN_MODE_NONE).assertTrue();
      scRestoreName(prev);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_STATE_PROFILE_A2DP_NAME_PAIRED
     * @tc.name quad_enable_state_profile_a2dp_name_paired
     * @tc.desc 4-way: enable -> state -> profile a2dp -> name -> paired
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_state_profile_a2dp_name_paired', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      assertLocalOk(scInvoke(() => {
        connection.getProfileConnectionState(constant.ProfileId.PROFILE_A2DP_SOURCE);
      }));
      const prevN = scGetLocalName();
      const wantN = 'ScProfName';
      scAssertNameRoundTrip(scSetLocalName(wantN), wantN);
      let listN: string[] = [];
      assertLocalOk(scInvoke(() => {
        listN = connection.getPairedDevices();
      }));
      expect(listN !== null).assertTrue();
      scRestoreName(prevN);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_STATE_PROFILE_HFP_NAME_PAIRED
     * @tc.name quad_enable_state_profile_hfp_name_paired
     * @tc.desc 4-way: enable -> state -> profile hfp -> name -> paired
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_state_profile_hfp_name_paired', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      assertLocalOk(scInvoke(() => {
        connection.getProfileConnectionState(constant.ProfileId.PROFILE_HANDSFREE_AUDIO_GATEWAY);
      }));
      const prevN = scGetLocalName();
      const wantN = 'ScProfName';
      scAssertNameRoundTrip(scSetLocalName(wantN), wantN);
      let listN: string[] = [];
      assertLocalOk(scInvoke(() => {
        listN = connection.getPairedDevices();
      }));
      expect(listN !== null).assertTrue();
      scRestoreName(prevN);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_STATE_PROFILE_HID_NAME_PAIRED
     * @tc.name quad_enable_state_profile_hid_name_paired
     * @tc.desc 4-way: enable -> state -> profile hid -> name -> paired
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_state_profile_hid_name_paired', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      assertLocalOk(scInvoke(() => {
        connection.getProfileConnectionState(constant.ProfileId.PROFILE_HID_HOST);
      }));
      const prevN = scGetLocalName();
      const wantN = 'ScProfName';
      scAssertNameRoundTrip(scSetLocalName(wantN), wantN);
      let listN: string[] = [];
      assertLocalOk(scInvoke(() => {
        listN = connection.getPairedDevices();
      }));
      expect(listN !== null).assertTrue();
      scRestoreName(prevN);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_ENABLE_STATE_PROFILE_PAN_NAME_PAIRED
     * @tc.name quad_enable_state_profile_pan_name_paired
     * @tc.desc 4-way: enable -> state -> profile pan -> name -> paired
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_enable_state_profile_pan_name_paired', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());
      assertLocalOk(scInvoke(() => {
        connection.getProfileConnectionState(constant.ProfileId.PROFILE_PAN_NETWORK);
      }));
      const prevN = scGetLocalName();
      const wantN = 'ScProfName';
      scAssertNameRoundTrip(scSetLocalName(wantN), wantN);
      let listN: string[] = [];
      assertLocalOk(scInvoke(() => {
        listN = connection.getPairedDevices();
      }));
      expect(listN !== null).assertTrue();
      scRestoreName(prevN);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_QUAD_POWER_CYCLE_X2
     * @tc.name quad_power_cycle_x2
     * @tc.desc 4-way repeated power cycle x2 then verify ON|TURNING_ON
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('quad_power_cycle_x2', 0, async () => {
      for (let i = 0; i < 2; i++) {
        LogUtil.info('quad_power round=' + i);
        assertLocalOk(scDisable());
        assertLocalOk(scEnable());
        scAssertStateOn(await scAwaitStateOn(2500));
      }
      scAssertStateOn(scGetState());
      }, 45000);

  });
}
