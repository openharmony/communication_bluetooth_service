/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Auto-generated by skills/ohtest/bluetooth_full_coverage.py (do not edit by hand).
 * Suite: FullCoverage_legacy_bluetooth_01
 * Scenarios per API: n=normal, e=error, b=boundary, r=return, x=repeat, c=condition.
 * Direct API calls only (no fcRun wrappers); LogUtil + real return/error assertions.
 */
import { describe, beforeEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import { BusinessError } from '@ohos.base';
import bluetooth from '@ohos.bluetooth';
import {
  BADDR,
  BAD_MAC,
  EMPTY_MAC,
  SVC_UUID,
  CHAR_UUID,
  bleCharRef,
  bleDescRef,
  bleGattService,
  bleNotifyChar,
  blePhyValue,
  gattConnParam,
  hidSdpStub,
  hidQosStub,
  sppOpt,
  legacySppOpt,
  bleAp,
  bleAd,
  bleAs,
  buf0,
  partnerDeviceAddr,
  partnerDeviceCap,
  partnerBusinessCap,
  gattServerResponse,
  ctrlDeviceActionParams,
  trustedPairedDevices,
  sysBleStartOpt,
  sysBleStopOpt,
  sysBleFoundOpt,
  assertLocalOk,
  assertPeerExpect,
  assertThrownErrIn,
  BT_DOC_ERR_CODES,
  ensureBtOn,
  runPerf,
  runStress,
  waitCallback
} from './ApitestFullCoverageShared';

export default function bluetoothFullCoverageLegacy01_p1() {
  describe('FullCoverage_legacy_bluetooth_01_p1', () => {
    beforeEach(() => {
    ensureBtOn();
    });
describe('leg_bt_pairDevice', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_PAIRDEVICE_N
     * @tc.name leg_bt_pairDevice_n
     * @tc.desc Verify Bluetooth API leg bt pairDevice normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_pairDevice_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.pairDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_PAIRDEVICE_E
     * @tc.name leg_bt_pairDevice_e
     * @tc.desc Verify Bluetooth API leg bt pairDevice error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_pairDevice_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.pairDevice(EMPTY_MAC);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_PAIRDEVICE_B
     * @tc.name leg_bt_pairDevice_b
     * @tc.desc Verify Bluetooth API leg bt pairDevice boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_pairDevice_b', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.pairDevice(BAD_MAC);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_PAIRDEVICE_R
     * @tc.name leg_bt_pairDevice_r
     * @tc.desc Verify Bluetooth API leg bt pairDevice return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_pairDevice_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.pairDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_PAIRDEVICE_X
     * @tc.name leg_bt_pairDevice_x
     * @tc.desc Verify Bluetooth API leg bt pairDevice repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_pairDevice_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.pairDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.pairDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_PAIRDEVICE_C
     * @tc.name leg_bt_pairDevice_c
     * @tc.desc Verify Bluetooth API leg bt pairDevice condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_pairDevice_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.pairDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_PAIRDEVICE_PERF
     * @tc.name leg_bt_pairDevice_perf
     * @tc.desc Verify Bluetooth apitest case leg bt pairDevice perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_pairDevice_perf', 0, () => {
      runPerf('leg_bt_pairDevice_perf', 5000, () => {
        try {
          bluetooth.pairDevice(BADDR);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_PAIRDEVICE_STRESS
     * @tc.name leg_bt_pairDevice_stress
     * @tc.desc Verify Bluetooth apitest case leg bt pairDevice stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_pairDevice_stress', 0, () => {
      runStress('leg_bt_pairDevice_stress', 20, () => {
        try {
          bluetooth.pairDevice(BADDR);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('leg_bt_cancelPairedDevice', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_CANCELPAIREDDEVICE_N
     * @tc.name leg_bt_cancelPairedDevice_n
     * @tc.desc Verify Bluetooth API leg bt cancelPairedDevice normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_cancelPairedDevice_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.cancelPairedDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_CANCELPAIREDDEVICE_E
     * @tc.name leg_bt_cancelPairedDevice_e
     * @tc.desc Verify Bluetooth API leg bt cancelPairedDevice error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_cancelPairedDevice_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.cancelPairedDevice(EMPTY_MAC);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_CANCELPAIREDDEVICE_B
     * @tc.name leg_bt_cancelPairedDevice_b
     * @tc.desc Verify Bluetooth API leg bt cancelPairedDevice boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_cancelPairedDevice_b', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.cancelPairedDevice(BAD_MAC);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_CANCELPAIREDDEVICE_R
     * @tc.name leg_bt_cancelPairedDevice_r
     * @tc.desc Verify Bluetooth API leg bt cancelPairedDevice return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_cancelPairedDevice_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.cancelPairedDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_CANCELPAIREDDEVICE_X
     * @tc.name leg_bt_cancelPairedDevice_x
     * @tc.desc Verify Bluetooth API leg bt cancelPairedDevice repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_cancelPairedDevice_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.cancelPairedDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.cancelPairedDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_CANCELPAIREDDEVICE_C
     * @tc.name leg_bt_cancelPairedDevice_c
     * @tc.desc Verify Bluetooth API leg bt cancelPairedDevice condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_cancelPairedDevice_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.cancelPairedDevice(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_CANCELPAIREDDEVICE_PERF
     * @tc.name leg_bt_cancelPairedDevice_perf
     * @tc.desc Verify Bluetooth apitest case leg bt cancelPairedDevice perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_cancelPairedDevice_perf', 0, () => {
      runPerf('leg_bt_cancelPairedDevice_perf', 5000, () => {
        try {
          bluetooth.cancelPairedDevice(BADDR);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_CANCELPAIREDDEVICE_STRESS
     * @tc.name leg_bt_cancelPairedDevice_stress
     * @tc.desc Verify Bluetooth apitest case leg bt cancelPairedDevice stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_cancelPairedDevice_stress', 0, () => {
      runStress('leg_bt_cancelPairedDevice_stress', 20, () => {
        try {
          bluetooth.cancelPairedDevice(BADDR);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('leg_bt_getRemoteDeviceClass', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_GETREMOTEDEVICECLASS_N
     * @tc.name leg_bt_getRemoteDeviceClass_n
     * @tc.desc Verify Bluetooth API leg bt getRemoteDeviceClass normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_getRemoteDeviceClass_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.getRemoteDeviceClass(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_GETREMOTEDEVICECLASS_E
     * @tc.name leg_bt_getRemoteDeviceClass_e
     * @tc.desc Verify Bluetooth API leg bt getRemoteDeviceClass error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_getRemoteDeviceClass_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.getRemoteDeviceClass(EMPTY_MAC);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_GETREMOTEDEVICECLASS_B
     * @tc.name leg_bt_getRemoteDeviceClass_b
     * @tc.desc Verify Bluetooth API leg bt getRemoteDeviceClass boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_getRemoteDeviceClass_b', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.getRemoteDeviceClass(BAD_MAC);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_GETREMOTEDEVICECLASS_R
     * @tc.name leg_bt_getRemoteDeviceClass_r
     * @tc.desc Verify Bluetooth API leg bt getRemoteDeviceClass return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_getRemoteDeviceClass_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.getRemoteDeviceClass(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_GETREMOTEDEVICECLASS_X
     * @tc.name leg_bt_getRemoteDeviceClass_x
     * @tc.desc Verify Bluetooth API leg bt getRemoteDeviceClass repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_getRemoteDeviceClass_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.getRemoteDeviceClass(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.getRemoteDeviceClass(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_GETREMOTEDEVICECLASS_C
     * @tc.name leg_bt_getRemoteDeviceClass_c
     * @tc.desc Verify Bluetooth API leg bt getRemoteDeviceClass condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_getRemoteDeviceClass_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.getRemoteDeviceClass(BADDR);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_GETREMOTEDEVICECLASS_PERF
     * @tc.name leg_bt_getRemoteDeviceClass_perf
     * @tc.desc Verify Bluetooth apitest case leg bt getRemoteDeviceClass perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_getRemoteDeviceClass_perf', 0, () => {
      runPerf('leg_bt_getRemoteDeviceClass_perf', 5000, () => {
        try {
          bluetooth.getRemoteDeviceClass(BADDR);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_GETREMOTEDEVICECLASS_STRESS
     * @tc.name leg_bt_getRemoteDeviceClass_stress
     * @tc.desc Verify Bluetooth apitest case leg bt getRemoteDeviceClass stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_getRemoteDeviceClass_stress', 0, () => {
      runStress('leg_bt_getRemoteDeviceClass_stress', 20, () => {
        try {
          bluetooth.getRemoteDeviceClass(BADDR);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('leg_bt_enableBluetooth', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_ENABLEBLUETOOTH_N
     * @tc.name leg_bt_enableBluetooth_n
     * @tc.desc Verify Bluetooth API leg bt enableBluetooth normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_ENABLEBLUETOOTH_E
     * @tc.name leg_bt_enableBluetooth_e
     * @tc.desc Verify Bluetooth API leg bt enableBluetooth error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.enableBluetooth();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_ENABLEBLUETOOTH_B
     * @tc.name leg_bt_enableBluetooth_b
     * @tc.desc Verify Bluetooth API leg bt enableBluetooth boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_b', 0, () => {
      let errCode = 0;
      LogUtil.info('b: invoke');
      try {
        bluetooth.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_ENABLEBLUETOOTH_R
     * @tc.name leg_bt_enableBluetooth_r
     * @tc.desc Verify Bluetooth API leg bt enableBluetooth return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_ENABLEBLUETOOTH_X
     * @tc.name leg_bt_enableBluetooth_x
     * @tc.desc Verify Bluetooth API leg bt enableBluetooth repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_ENABLEBLUETOOTH_C
     * @tc.name leg_bt_enableBluetooth_c
     * @tc.desc Verify Bluetooth API leg bt enableBluetooth condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.enableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_ENABLEBLUETOOTH_PERF
     * @tc.name leg_bt_enableBluetooth_perf
     * @tc.desc Verify Bluetooth apitest case leg bt enableBluetooth perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_perf', 0, () => {
      runPerf('leg_bt_enableBluetooth_perf', 5000, () => {
        try {
          bluetooth.enableBluetooth();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_ENABLEBLUETOOTH_STRESS
     * @tc.name leg_bt_enableBluetooth_stress
     * @tc.desc Verify Bluetooth apitest case leg bt enableBluetooth stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_enableBluetooth_stress', 0, () => {
      runStress('leg_bt_enableBluetooth_stress', 20, () => {
        try {
          bluetooth.enableBluetooth();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('leg_bt_disableBluetooth', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_DISABLEBLUETOOTH_N
     * @tc.name leg_bt_disableBluetooth_n
     * @tc.desc Verify Bluetooth API leg bt disableBluetooth normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_disableBluetooth_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_DISABLEBLUETOOTH_E
     * @tc.name leg_bt_disableBluetooth_e
     * @tc.desc Verify Bluetooth API leg bt disableBluetooth error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_disableBluetooth_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.disableBluetooth();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_DISABLEBLUETOOTH_B
     * @tc.name leg_bt_disableBluetooth_b
     * @tc.desc Verify Bluetooth API leg bt disableBluetooth boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_disableBluetooth_b', 0, () => {
      let errCode = 0;
      LogUtil.info('b: invoke');
      try {
        bluetooth.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_DISABLEBLUETOOTH_R
     * @tc.name leg_bt_disableBluetooth_r
     * @tc.desc Verify Bluetooth API leg bt disableBluetooth return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_disableBluetooth_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_DISABLEBLUETOOTH_X
     * @tc.name leg_bt_disableBluetooth_x
     * @tc.desc Verify Bluetooth API leg bt disableBluetooth repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_disableBluetooth_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_DISABLEBLUETOOTH_C
     * @tc.name leg_bt_disableBluetooth_c
     * @tc.desc Verify Bluetooth API leg bt disableBluetooth condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_disableBluetooth_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.disableBluetooth();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_DISABLEBLUETOOTH_PERF
     * @tc.name leg_bt_disableBluetooth_perf
     * @tc.desc Verify Bluetooth apitest case leg bt disableBluetooth perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_disableBluetooth_perf', 0, () => {
      runPerf('leg_bt_disableBluetooth_perf', 5000, () => {
        try {
          bluetooth.disableBluetooth();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_DISABLEBLUETOOTH_STRESS
     * @tc.name leg_bt_disableBluetooth_stress
     * @tc.desc Verify Bluetooth apitest case leg bt disableBluetooth stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_disableBluetooth_stress', 0, () => {
      runStress('leg_bt_disableBluetooth_stress', 20, () => {
        try {
          bluetooth.disableBluetooth();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('leg_bt_setDevicePairingConfirmation', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETDEVICEPAIRINGCONFIRMATION_N
     * @tc.name leg_bt_setDevicePairingConfirmation_n
     * @tc.desc Verify Bluetooth API leg bt setDevicePairingConfirmation normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setDevicePairingConfirmation_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.setDevicePairingConfirmation(BADDR, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETDEVICEPAIRINGCONFIRMATION_E
     * @tc.name leg_bt_setDevicePairingConfirmation_e
     * @tc.desc Verify Bluetooth API leg bt setDevicePairingConfirmation error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setDevicePairingConfirmation_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.setDevicePairingConfirmation(EMPTY_MAC, false);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETDEVICEPAIRINGCONFIRMATION_B
     * @tc.name leg_bt_setDevicePairingConfirmation_b
     * @tc.desc Verify Bluetooth API leg bt setDevicePairingConfirmation boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setDevicePairingConfirmation_b', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.setDevicePairingConfirmation(BAD_MAC, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETDEVICEPAIRINGCONFIRMATION_R
     * @tc.name leg_bt_setDevicePairingConfirmation_r
     * @tc.desc Verify Bluetooth API leg bt setDevicePairingConfirmation return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setDevicePairingConfirmation_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.setDevicePairingConfirmation(BADDR, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETDEVICEPAIRINGCONFIRMATION_X
     * @tc.name leg_bt_setDevicePairingConfirmation_x
     * @tc.desc Verify Bluetooth API leg bt setDevicePairingConfirmation repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setDevicePairingConfirmation_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.setDevicePairingConfirmation(BADDR, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.setDevicePairingConfirmation(BADDR, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETDEVICEPAIRINGCONFIRMATION_C
     * @tc.name leg_bt_setDevicePairingConfirmation_c
     * @tc.desc Verify Bluetooth API leg bt setDevicePairingConfirmation condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setDevicePairingConfirmation_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.setDevicePairingConfirmation(BADDR, false);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETDEVICEPAIRINGCONFIRMATION_PERF
     * @tc.name leg_bt_setDevicePairingConfirmation_perf
     * @tc.desc Verify Bluetooth apitest case leg bt setDevicePairingConfirmation perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setDevicePairingConfirmation_perf', 0, () => {
      runPerf('leg_bt_setDevicePairingConfirmation_perf', 5000, () => {
        try {
          bluetooth.setDevicePairingConfirmation(BADDR, true);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETDEVICEPAIRINGCONFIRMATION_STRESS
     * @tc.name leg_bt_setDevicePairingConfirmation_stress
     * @tc.desc Verify Bluetooth apitest case leg bt setDevicePairingConfirmation stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setDevicePairingConfirmation_stress', 0, () => {
      runStress('leg_bt_setDevicePairingConfirmation_stress', 20, () => {
        try {
          bluetooth.setDevicePairingConfirmation(BADDR, true);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('leg_bt_setLocalName', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETLOCALNAME_N
     * @tc.name leg_bt_setLocalName_n
     * @tc.desc Verify Bluetooth API leg bt setLocalName normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setLocalName_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.setLocalName('hypium');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETLOCALNAME_E
     * @tc.name leg_bt_setLocalName_e
     * @tc.desc Verify Bluetooth API leg bt setLocalName error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setLocalName_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.setLocalName('');
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETLOCALNAME_B
     * @tc.name leg_bt_setLocalName_b
     * @tc.desc Verify Bluetooth API leg bt setLocalName boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setLocalName_b', 0, () => {
      let errCode = 0;
      LogUtil.info('b: invoke');
      try {
        bluetooth.setLocalName('hypium');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETLOCALNAME_R
     * @tc.name leg_bt_setLocalName_r
     * @tc.desc Verify Bluetooth API leg bt setLocalName return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setLocalName_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.setLocalName('hypium');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETLOCALNAME_X
     * @tc.name leg_bt_setLocalName_x
     * @tc.desc Verify Bluetooth API leg bt setLocalName repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setLocalName_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.setLocalName('hypium');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.setLocalName('hypium');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETLOCALNAME_C
     * @tc.name leg_bt_setLocalName_c
     * @tc.desc Verify Bluetooth API leg bt setLocalName condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setLocalName_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.setLocalName('hypium');
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETLOCALNAME_PERF
     * @tc.name leg_bt_setLocalName_perf
     * @tc.desc Verify Bluetooth apitest case leg bt setLocalName perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setLocalName_perf', 0, () => {
      runPerf('leg_bt_setLocalName_perf', 5000, () => {
        try {
          bluetooth.setLocalName('hypium');
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETLOCALNAME_STRESS
     * @tc.name leg_bt_setLocalName_stress
     * @tc.desc Verify Bluetooth apitest case leg bt setLocalName stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setLocalName_stress', 0, () => {
      runStress('leg_bt_setLocalName_stress', 20, () => {
        try {
          bluetooth.setLocalName('hypium');
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('leg_bt_setBluetoothScanMode', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETBLUETOOTHSCANMODE_N
     * @tc.name leg_bt_setBluetoothScanMode_n
     * @tc.desc Verify Bluetooth API leg bt setBluetoothScanMode normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setBluetoothScanMode_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.setBluetoothScanMode(4, 0);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETBLUETOOTHSCANMODE_E
     * @tc.name leg_bt_setBluetoothScanMode_e
     * @tc.desc Verify Bluetooth API leg bt setBluetoothScanMode error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setBluetoothScanMode_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.setBluetoothScanMode(0, -1);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETBLUETOOTHSCANMODE_B
     * @tc.name leg_bt_setBluetoothScanMode_b
     * @tc.desc Verify Bluetooth API leg bt setBluetoothScanMode boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setBluetoothScanMode_b', 0, () => {
      let errCode = 0;
      LogUtil.info('b: invoke');
      try {
        bluetooth.setBluetoothScanMode(4, 0);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETBLUETOOTHSCANMODE_R
     * @tc.name leg_bt_setBluetoothScanMode_r
     * @tc.desc Verify Bluetooth API leg bt setBluetoothScanMode return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setBluetoothScanMode_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.setBluetoothScanMode(4, 0);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETBLUETOOTHSCANMODE_X
     * @tc.name leg_bt_setBluetoothScanMode_x
     * @tc.desc Verify Bluetooth API leg bt setBluetoothScanMode repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setBluetoothScanMode_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.setBluetoothScanMode(4, 0);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.setBluetoothScanMode(4, 0);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETBLUETOOTHSCANMODE_C
     * @tc.name leg_bt_setBluetoothScanMode_c
     * @tc.desc Verify Bluetooth API leg bt setBluetoothScanMode condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setBluetoothScanMode_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.setBluetoothScanMode(4, 0);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETBLUETOOTHSCANMODE_PERF
     * @tc.name leg_bt_setBluetoothScanMode_perf
     * @tc.desc Verify Bluetooth apitest case leg bt setBluetoothScanMode perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setBluetoothScanMode_perf', 0, () => {
      runPerf('leg_bt_setBluetoothScanMode_perf', 5000, () => {
        try {
          bluetooth.setBluetoothScanMode(4, 0);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_SETBLUETOOTHSCANMODE_STRESS
     * @tc.name leg_bt_setBluetoothScanMode_stress
     * @tc.desc Verify Bluetooth apitest case leg bt setBluetoothScanMode stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_setBluetoothScanMode_stress', 0, () => {
      runStress('leg_bt_setBluetoothScanMode_stress', 20, () => {
        try {
          bluetooth.setBluetoothScanMode(4, 0);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('leg_bt_startBluetoothDiscovery', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_STARTBLUETOOTHDISCOVERY_N
     * @tc.name leg_bt_startBluetoothDiscovery_n
     * @tc.desc Verify Bluetooth API leg bt startBluetoothDiscovery normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_startBluetoothDiscovery_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.startBluetoothDiscovery();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_STARTBLUETOOTHDISCOVERY_E
     * @tc.name leg_bt_startBluetoothDiscovery_e
     * @tc.desc Verify Bluetooth API leg bt startBluetoothDiscovery error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_startBluetoothDiscovery_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        bluetooth.startBluetoothDiscovery();
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_STARTBLUETOOTHDISCOVERY_B
     * @tc.name leg_bt_startBluetoothDiscovery_b
     * @tc.desc Verify Bluetooth API leg bt startBluetoothDiscovery boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_startBluetoothDiscovery_b', 0, () => {
      let errCode = 0;
      LogUtil.info('b: invoke');
      try {
        bluetooth.startBluetoothDiscovery();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_STARTBLUETOOTHDISCOVERY_R
     * @tc.name leg_bt_startBluetoothDiscovery_r
     * @tc.desc Verify Bluetooth API leg bt startBluetoothDiscovery return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_startBluetoothDiscovery_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        bluetooth.startBluetoothDiscovery();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_STARTBLUETOOTHDISCOVERY_X
     * @tc.name leg_bt_startBluetoothDiscovery_x
     * @tc.desc Verify Bluetooth API leg bt startBluetoothDiscovery repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_startBluetoothDiscovery_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        bluetooth.startBluetoothDiscovery();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        bluetooth.startBluetoothDiscovery();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_STARTBLUETOOTHDISCOVERY_C
     * @tc.name leg_bt_startBluetoothDiscovery_c
     * @tc.desc Verify Bluetooth API leg bt startBluetoothDiscovery condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_startBluetoothDiscovery_c', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        bluetooth.startBluetoothDiscovery();
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_STARTBLUETOOTHDISCOVERY_PERF
     * @tc.name leg_bt_startBluetoothDiscovery_perf
     * @tc.desc Verify Bluetooth apitest case leg bt startBluetoothDiscovery perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_startBluetoothDiscovery_perf', 0, () => {
      runPerf('leg_bt_startBluetoothDiscovery_perf', 5000, () => {
        try {
          bluetooth.startBluetoothDiscovery();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_LEG_BT_STARTBLUETOOTHDISCOVERY_STRESS
     * @tc.name leg_bt_startBluetoothDiscovery_stress
     * @tc.desc Verify Bluetooth apitest case leg bt startBluetoothDiscovery stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('leg_bt_startBluetoothDiscovery_stress', 0, () => {
      runStress('leg_bt_startBluetoothDiscovery_stress', 20, () => {
        try {
          bluetooth.startBluetoothDiscovery();
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
  });
}
