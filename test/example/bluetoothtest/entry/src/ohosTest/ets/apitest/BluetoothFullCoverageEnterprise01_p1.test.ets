/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Auto-generated by skills/ohtest/bluetooth_full_coverage.py (do not edit by hand).
 * Suite: FullCoverage_enterprise_partner
 * Scenarios per API: n=normal, e=error, b=boundary, r=return, x=repeat, c=condition.
 * Direct API calls only (no fcRun wrappers); LogUtil + real return/error assertions.
 */
import { describe, beforeEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import { BusinessError } from '@ohos.base';
import enterpriseBt from '@ohos.enterprise.bluetoothManager';
import partnerAgent from '@ohos.FusionConnectivity.partnerAgent';
import {
  BADDR,
  BAD_MAC,
  EMPTY_MAC,
  SVC_UUID,
  CHAR_UUID,
  bleCharRef,
  bleDescRef,
  bleGattService,
  bleNotifyChar,
  blePhyValue,
  gattConnParam,
  hidSdpStub,
  hidQosStub,
  sppOpt,
  legacySppOpt,
  bleAp,
  bleAd,
  bleAs,
  buf0,
  partnerDeviceAddr,
  partnerDeviceCap,
  partnerBusinessCap,
  gattServerResponse,
  ctrlDeviceActionParams,
  trustedPairedDevices,
  sysBleStartOpt,
  sysBleStopOpt,
  sysBleFoundOpt,
  assertLocalOk,
  assertPeerExpect,
  assertThrownErrIn,
  BT_DOC_ERR_CODES,
  ensureBtOn,
  runPerf,
  runStress,
  waitCallback
} from './ApitestFullCoverageShared';

export default function bluetoothFullCoverageEnterprise01_p1() {
  describe('FullCoverage_enterprise_partner_p1', () => {
    beforeEach(() => {
    ensureBtOn();
    });
describe('ent_getBluetoothInfo', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETBLUETOOTHINFO_N
     * @tc.name ent_getBluetoothInfo_n
     * @tc.desc Verify Bluetooth API ent getBluetoothInfo normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getBluetoothInfo_n', 0, () => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('n: invoke');
      try {
        value = enterpriseBt.getBluetoothInfo({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(value !== null && value !== undefined).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETBLUETOOTHINFO_E
     * @tc.name ent_getBluetoothInfo_e
     * @tc.desc Verify Bluetooth API ent getBluetoothInfo error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getBluetoothInfo_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        enterpriseBt.getBluetoothInfo({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETBLUETOOTHINFO_B
     * @tc.name ent_getBluetoothInfo_b
     * @tc.desc Verify Bluetooth API ent getBluetoothInfo boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getBluetoothInfo_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        enterpriseBt.getBluetoothInfo({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETBLUETOOTHINFO_R
     * @tc.name ent_getBluetoothInfo_r
     * @tc.desc Verify Bluetooth API ent getBluetoothInfo return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getBluetoothInfo_r', 0, () => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('r: invoke');
      try {
        value = enterpriseBt.getBluetoothInfo({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(value !== null && value !== undefined).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETBLUETOOTHINFO_X
     * @tc.name ent_getBluetoothInfo_x
     * @tc.desc Verify Bluetooth API ent getBluetoothInfo repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getBluetoothInfo_x', 0, () => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('x1: invoke');
      try {
        value = enterpriseBt.getBluetoothInfo({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(value !== null && value !== undefined).assertTrue();
      }
      errCode = 0;
      value = null;
      LogUtil.info('x2: invoke');
      try {
        value = enterpriseBt.getBluetoothInfo({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(value !== null && value !== undefined).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETBLUETOOTHINFO_C
     * @tc.name ent_getBluetoothInfo_c
     * @tc.desc Verify Bluetooth API ent getBluetoothInfo condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getBluetoothInfo_c', 0, () => {
      let errCode = 0;
      let value: Object | null = null;
      LogUtil.info('c: invoke');
      try {
        value = enterpriseBt.getBluetoothInfo({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Object;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(value !== null && value !== undefined).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETBLUETOOTHINFO_PERF
     * @tc.name ent_getBluetoothInfo_perf
     * @tc.desc Verify Bluetooth apitest case ent getBluetoothInfo perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getBluetoothInfo_perf', 0, () => {
      runPerf('ent_getBluetoothInfo_perf', 5000, () => {
        try {
          enterpriseBt.getBluetoothInfo({bundleName:'com.example.test',
            abilityName:'EntryAbility'}) as Object;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETBLUETOOTHINFO_STRESS
     * @tc.name ent_getBluetoothInfo_stress
     * @tc.desc Verify Bluetooth apitest case ent getBluetoothInfo stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getBluetoothInfo_stress', 0, () => {
      runStress('ent_getBluetoothInfo_stress', 20, () => {
        try {
          enterpriseBt.getBluetoothInfo({bundleName:'com.example.test',
            abilityName:'EntryAbility'}) as Object;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ent_setBluetoothDisabled', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_SETBLUETOOTHDISABLED_N
     * @tc.name ent_setBluetoothDisabled_n
     * @tc.desc Verify Bluetooth API ent setBluetoothDisabled normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_setBluetoothDisabled_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        enterpriseBt.setBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'}, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_SETBLUETOOTHDISABLED_E
     * @tc.name ent_setBluetoothDisabled_e
     * @tc.desc Verify Bluetooth API ent setBluetoothDisabled error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_setBluetoothDisabled_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        enterpriseBt.setBluetoothDisabled({bundleName:'', abilityName:''}, true);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_SETBLUETOOTHDISABLED_B
     * @tc.name ent_setBluetoothDisabled_b
     * @tc.desc Verify Bluetooth API ent setBluetoothDisabled boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_setBluetoothDisabled_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        enterpriseBt.setBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'}, false);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_SETBLUETOOTHDISABLED_R
     * @tc.name ent_setBluetoothDisabled_r
     * @tc.desc Verify Bluetooth API ent setBluetoothDisabled return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_setBluetoothDisabled_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        enterpriseBt.setBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'}, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_SETBLUETOOTHDISABLED_X
     * @tc.name ent_setBluetoothDisabled_x
     * @tc.desc Verify Bluetooth API ent setBluetoothDisabled repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_setBluetoothDisabled_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        enterpriseBt.setBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'}, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        enterpriseBt.setBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'}, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_SETBLUETOOTHDISABLED_C
     * @tc.name ent_setBluetoothDisabled_c
     * @tc.desc Verify Bluetooth API ent setBluetoothDisabled condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_setBluetoothDisabled_c', 0, () => {
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        enterpriseBt.setBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'}, true);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_SETBLUETOOTHDISABLED_PERF
     * @tc.name ent_setBluetoothDisabled_perf
     * @tc.desc Verify Bluetooth apitest case ent setBluetoothDisabled perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_setBluetoothDisabled_perf', 0, () => {
      runPerf('ent_setBluetoothDisabled_perf', 5000, () => {
        try {
          enterpriseBt.setBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'},
            true);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_SETBLUETOOTHDISABLED_STRESS
     * @tc.name ent_setBluetoothDisabled_stress
     * @tc.desc Verify Bluetooth apitest case ent setBluetoothDisabled stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_setBluetoothDisabled_stress', 0, () => {
      runStress('ent_setBluetoothDisabled_stress', 20, () => {
        try {
          enterpriseBt.setBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'},
            true);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ent_isBluetoothDisabled', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ISBLUETOOTHDISABLED_N
     * @tc.name ent_isBluetoothDisabled_n
     * @tc.desc Verify Bluetooth API ent isBluetoothDisabled normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_isBluetoothDisabled_n', 0, () => {
      let errCode = 0;
      let value = false;
      LogUtil.info('n: invoke');
      try {
        value = enterpriseBt.isBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ISBLUETOOTHDISABLED_E
     * @tc.name ent_isBluetoothDisabled_e
     * @tc.desc Verify Bluetooth API ent isBluetoothDisabled error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_isBluetoothDisabled_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        enterpriseBt.isBluetoothDisabled({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ISBLUETOOTHDISABLED_B
     * @tc.name ent_isBluetoothDisabled_b
     * @tc.desc Verify Bluetooth API ent isBluetoothDisabled boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_isBluetoothDisabled_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        enterpriseBt.isBluetoothDisabled({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ISBLUETOOTHDISABLED_R
     * @tc.name ent_isBluetoothDisabled_r
     * @tc.desc Verify Bluetooth API ent isBluetoothDisabled return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_isBluetoothDisabled_r', 0, () => {
      let errCode = 0;
      let value = false;
      LogUtil.info('r: invoke');
      try {
        value = enterpriseBt.isBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ISBLUETOOTHDISABLED_X
     * @tc.name ent_isBluetoothDisabled_x
     * @tc.desc Verify Bluetooth API ent isBluetoothDisabled repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_isBluetoothDisabled_x', 0, () => {
      let errCode = 0;
      let value = false;
      LogUtil.info('x1: invoke');
      try {
        value = enterpriseBt.isBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
      errCode = 0;
      value = false;
      LogUtil.info('x2: invoke');
      try {
        value = enterpriseBt.isBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ISBLUETOOTHDISABLED_C
     * @tc.name ent_isBluetoothDisabled_c
     * @tc.desc Verify Bluetooth API ent isBluetoothDisabled condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_isBluetoothDisabled_c', 0, () => {
      let errCode = 0;
      let value = false;
      LogUtil.info('c: invoke');
      try {
        value = enterpriseBt.isBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(typeof value).assertEqual('boolean');
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ISBLUETOOTHDISABLED_PERF
     * @tc.name ent_isBluetoothDisabled_perf
     * @tc.desc Verify Bluetooth apitest case ent isBluetoothDisabled perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_isBluetoothDisabled_perf', 0, () => {
      runPerf('ent_isBluetoothDisabled_perf', 5000, () => {
        try {
          enterpriseBt.isBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'});
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ISBLUETOOTHDISABLED_STRESS
     * @tc.name ent_isBluetoothDisabled_stress
     * @tc.desc Verify Bluetooth apitest case ent isBluetoothDisabled stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_isBluetoothDisabled_stress', 0, () => {
      runStress('ent_isBluetoothDisabled_stress', 20, () => {
        try {
          enterpriseBt.isBluetoothDisabled({bundleName:'com.example.test', abilityName:'EntryAbility'});
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ent_addAllowedBluetoothDevices', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ADDALLOWEDBLUETOOTHDEVICES_N
     * @tc.name ent_addAllowedBluetoothDevices_n
     * @tc.desc Verify Bluetooth API ent addAllowedBluetoothDevices normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_addAllowedBluetoothDevices_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        enterpriseBt.addAllowedBluetoothDevices({bundleName:'com.example.test', abilityName:'EntryAbility'},
          [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ADDALLOWEDBLUETOOTHDEVICES_E
     * @tc.name ent_addAllowedBluetoothDevices_e
     * @tc.desc Verify Bluetooth API ent addAllowedBluetoothDevices error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_addAllowedBluetoothDevices_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        enterpriseBt.addAllowedBluetoothDevices({bundleName:'', abilityName:''}, [BADDR]);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ADDALLOWEDBLUETOOTHDEVICES_B
     * @tc.name ent_addAllowedBluetoothDevices_b
     * @tc.desc Verify Bluetooth API ent addAllowedBluetoothDevices boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_addAllowedBluetoothDevices_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        enterpriseBt.addAllowedBluetoothDevices({bundleName:'com.example.test', abilityName:'EntryAbility'},
          [EMPTY_MAC]);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ADDALLOWEDBLUETOOTHDEVICES_R
     * @tc.name ent_addAllowedBluetoothDevices_r
     * @tc.desc Verify Bluetooth API ent addAllowedBluetoothDevices return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_addAllowedBluetoothDevices_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        enterpriseBt.addAllowedBluetoothDevices({bundleName:'com.example.test', abilityName:'EntryAbility'},
          [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ADDALLOWEDBLUETOOTHDEVICES_X
     * @tc.name ent_addAllowedBluetoothDevices_x
     * @tc.desc Verify Bluetooth API ent addAllowedBluetoothDevices repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_addAllowedBluetoothDevices_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        enterpriseBt.addAllowedBluetoothDevices({bundleName:'com.example.test', abilityName:'EntryAbility'},
          [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        enterpriseBt.addAllowedBluetoothDevices({bundleName:'com.example.test', abilityName:'EntryAbility'},
          [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ADDALLOWEDBLUETOOTHDEVICES_C
     * @tc.name ent_addAllowedBluetoothDevices_c
     * @tc.desc Verify Bluetooth API ent addAllowedBluetoothDevices condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_addAllowedBluetoothDevices_c', 0, () => {
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        enterpriseBt.addAllowedBluetoothDevices({bundleName:'com.example.test', abilityName:'EntryAbility'},
          [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ADDALLOWEDBLUETOOTHDEVICES_PERF
     * @tc.name ent_addAllowedBluetoothDevices_perf
     * @tc.desc Verify Bluetooth apitest case ent addAllowedBluetoothDevices perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_addAllowedBluetoothDevices_perf', 0, () => {
      runPerf('ent_addAllowedBluetoothDevices_perf', 5000, () => {
        try {
          enterpriseBt.addAllowedBluetoothDevices({bundleName:'com.example.test',
            abilityName:'EntryAbility'}, [BADDR]);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_ADDALLOWEDBLUETOOTHDEVICES_STRESS
     * @tc.name ent_addAllowedBluetoothDevices_stress
     * @tc.desc Verify Bluetooth apitest case ent addAllowedBluetoothDevices stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_addAllowedBluetoothDevices_stress', 0, () => {
      runStress('ent_addAllowedBluetoothDevices_stress', 20, () => {
        try {
          enterpriseBt.addAllowedBluetoothDevices({bundleName:'com.example.test',
            abilityName:'EntryAbility'}, [BADDR]);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ent_removeAllowedBluetoothDevices', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_REMOVEALLOWEDBLUETOOTHDEVICES_N
     * @tc.name ent_removeAllowedBluetoothDevices_n
     * @tc.desc Verify Bluetooth API ent removeAllowedBluetoothDevices normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_removeAllowedBluetoothDevices_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        enterpriseBt.removeAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}, [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_REMOVEALLOWEDBLUETOOTHDEVICES_E
     * @tc.name ent_removeAllowedBluetoothDevices_e
     * @tc.desc Verify Bluetooth API ent removeAllowedBluetoothDevices error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_removeAllowedBluetoothDevices_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        enterpriseBt.removeAllowedBluetoothDevices({bundleName:'', abilityName:''}, [BADDR]);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_REMOVEALLOWEDBLUETOOTHDEVICES_B
     * @tc.name ent_removeAllowedBluetoothDevices_b
     * @tc.desc Verify Bluetooth API ent removeAllowedBluetoothDevices boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_removeAllowedBluetoothDevices_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        enterpriseBt.removeAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}, [EMPTY_MAC]);
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_REMOVEALLOWEDBLUETOOTHDEVICES_R
     * @tc.name ent_removeAllowedBluetoothDevices_r
     * @tc.desc Verify Bluetooth API ent removeAllowedBluetoothDevices return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_removeAllowedBluetoothDevices_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        enterpriseBt.removeAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}, [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_REMOVEALLOWEDBLUETOOTHDEVICES_X
     * @tc.name ent_removeAllowedBluetoothDevices_x
     * @tc.desc Verify Bluetooth API ent removeAllowedBluetoothDevices repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_removeAllowedBluetoothDevices_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        enterpriseBt.removeAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}, [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        enterpriseBt.removeAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}, [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_REMOVEALLOWEDBLUETOOTHDEVICES_C
     * @tc.name ent_removeAllowedBluetoothDevices_c
     * @tc.desc Verify Bluetooth API ent removeAllowedBluetoothDevices condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_removeAllowedBluetoothDevices_c', 0, () => {
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        enterpriseBt.removeAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}, [BADDR]);
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_REMOVEALLOWEDBLUETOOTHDEVICES_PERF
     * @tc.name ent_removeAllowedBluetoothDevices_perf
     * @tc.desc Verify Bluetooth apitest case ent removeAllowedBluetoothDevices perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_removeAllowedBluetoothDevices_perf', 0, () => {
      runPerf('ent_removeAllowedBluetoothDevices_perf', 5000, () => {
        try {
          enterpriseBt.removeAllowedBluetoothDevices({bundleName:'com.example.test',
            abilityName:'EntryAbility'}, [BADDR]);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_REMOVEALLOWEDBLUETOOTHDEVICES_STRESS
     * @tc.name ent_removeAllowedBluetoothDevices_stress
     * @tc.desc Verify Bluetooth apitest case ent removeAllowedBluetoothDevices stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_removeAllowedBluetoothDevices_stress', 0, () => {
      runStress('ent_removeAllowedBluetoothDevices_stress', 20, () => {
        try {
          enterpriseBt.removeAllowedBluetoothDevices({bundleName:'com.example.test',
            abilityName:'EntryAbility'}, [BADDR]);
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ent_getAllowedBluetoothDevices', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETALLOWEDBLUETOOTHDEVICES_N
     * @tc.name ent_getAllowedBluetoothDevices_n
     * @tc.desc Verify Bluetooth API ent getAllowedBluetoothDevices normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getAllowedBluetoothDevices_n', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('n: invoke');
      try {
        value = enterpriseBt.getAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETALLOWEDBLUETOOTHDEVICES_E
     * @tc.name ent_getAllowedBluetoothDevices_e
     * @tc.desc Verify Bluetooth API ent getAllowedBluetoothDevices error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getAllowedBluetoothDevices_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        enterpriseBt.getAllowedBluetoothDevices({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETALLOWEDBLUETOOTHDEVICES_B
     * @tc.name ent_getAllowedBluetoothDevices_b
     * @tc.desc Verify Bluetooth API ent getAllowedBluetoothDevices boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getAllowedBluetoothDevices_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        enterpriseBt.getAllowedBluetoothDevices({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETALLOWEDBLUETOOTHDEVICES_R
     * @tc.name ent_getAllowedBluetoothDevices_r
     * @tc.desc Verify Bluetooth API ent getAllowedBluetoothDevices return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getAllowedBluetoothDevices_r', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('r: invoke');
      try {
        value = enterpriseBt.getAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETALLOWEDBLUETOOTHDEVICES_X
     * @tc.name ent_getAllowedBluetoothDevices_x
     * @tc.desc Verify Bluetooth API ent getAllowedBluetoothDevices repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getAllowedBluetoothDevices_x', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('x1: invoke');
      try {
        value = enterpriseBt.getAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
      errCode = 0;
      value = [];
      LogUtil.info('x2: invoke');
      try {
        value = enterpriseBt.getAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETALLOWEDBLUETOOTHDEVICES_C
     * @tc.name ent_getAllowedBluetoothDevices_c
     * @tc.desc Verify Bluetooth API ent getAllowedBluetoothDevices condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getAllowedBluetoothDevices_c', 0, () => {
      let errCode = 0;
      let value: Array < Object >  = [];
      LogUtil.info('c: invoke');
      try {
        value = enterpriseBt.getAllowedBluetoothDevices({bundleName:'com.example.test',
          abilityName:'EntryAbility'}) as Array < Object > ;
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode + ' value=' + JSON.stringify(value));
      assertPeerExpect(errCode);
      if (errCode === 0) {
        expect(Array.isArray(value)).assertTrue();
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETALLOWEDBLUETOOTHDEVICES_PERF
     * @tc.name ent_getAllowedBluetoothDevices_perf
     * @tc.desc Verify Bluetooth apitest case ent getAllowedBluetoothDevices perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getAllowedBluetoothDevices_perf', 0, () => {
      runPerf('ent_getAllowedBluetoothDevices_perf', 5000, () => {
        try {
          enterpriseBt.getAllowedBluetoothDevices({bundleName:'com.example.test',
            abilityName:'EntryAbility'}) as Array < Object > ;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_GETALLOWEDBLUETOOTHDEVICES_STRESS
     * @tc.name ent_getAllowedBluetoothDevices_stress
     * @tc.desc Verify Bluetooth apitest case ent getAllowedBluetoothDevices stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_getAllowedBluetoothDevices_stress', 0, () => {
      runStress('ent_getAllowedBluetoothDevices_stress', 20, () => {
        try {
          enterpriseBt.getAllowedBluetoothDevices({bundleName:'com.example.test',
            abilityName:'EntryAbility'}) as Array < Object > ;
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ent_turnOnBluetooth', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNONBLUETOOTH_N
     * @tc.name ent_turnOnBluetooth_n
     * @tc.desc Verify Bluetooth API ent turnOnBluetooth normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOnBluetooth_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        enterpriseBt.turnOnBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNONBLUETOOTH_E
     * @tc.name ent_turnOnBluetooth_e
     * @tc.desc Verify Bluetooth API ent turnOnBluetooth error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOnBluetooth_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        enterpriseBt.turnOnBluetooth({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNONBLUETOOTH_B
     * @tc.name ent_turnOnBluetooth_b
     * @tc.desc Verify Bluetooth API ent turnOnBluetooth boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOnBluetooth_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        enterpriseBt.turnOnBluetooth({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNONBLUETOOTH_R
     * @tc.name ent_turnOnBluetooth_r
     * @tc.desc Verify Bluetooth API ent turnOnBluetooth return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOnBluetooth_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        enterpriseBt.turnOnBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNONBLUETOOTH_X
     * @tc.name ent_turnOnBluetooth_x
     * @tc.desc Verify Bluetooth API ent turnOnBluetooth repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOnBluetooth_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        enterpriseBt.turnOnBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        enterpriseBt.turnOnBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNONBLUETOOTH_C
     * @tc.name ent_turnOnBluetooth_c
     * @tc.desc Verify Bluetooth API ent turnOnBluetooth condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOnBluetooth_c', 0, () => {
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        enterpriseBt.turnOnBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNONBLUETOOTH_PERF
     * @tc.name ent_turnOnBluetooth_perf
     * @tc.desc Verify Bluetooth apitest case ent turnOnBluetooth perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOnBluetooth_perf', 0, () => {
      runPerf('ent_turnOnBluetooth_perf', 5000, () => {
        try {
          enterpriseBt.turnOnBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNONBLUETOOTH_STRESS
     * @tc.name ent_turnOnBluetooth_stress
     * @tc.desc Verify Bluetooth apitest case ent turnOnBluetooth stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOnBluetooth_stress', 0, () => {
      runStress('ent_turnOnBluetooth_stress', 20, () => {
        try {
          enterpriseBt.turnOnBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
describe('ent_turnOffBluetooth', () => {

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNOFFBLUETOOTH_N
     * @tc.name ent_turnOffBluetooth_n
     * @tc.desc Verify Bluetooth API ent turnOffBluetooth normal scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOffBluetooth_n', 0, () => {
      let errCode = 0;
      LogUtil.info('n: invoke');
      try {
        enterpriseBt.turnOffBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('n: ' + JSON.stringify(err));
      }
      LogUtil.info('n: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNOFFBLUETOOTH_E
     * @tc.name ent_turnOffBluetooth_e
     * @tc.desc Verify Bluetooth API ent turnOffBluetooth error or exception scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOffBluetooth_e', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('e: invoke expect error');
      try {
        enterpriseBt.turnOffBluetooth({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('e: ' + JSON.stringify(err));
      }
      LogUtil.info('e: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNOFFBLUETOOTH_B
     * @tc.name ent_turnOffBluetooth_b
     * @tc.desc Verify Bluetooth API ent turnOffBluetooth boundary scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOffBluetooth_b', 0, () => {
      let threw = false;
      let errCode = 0;
      LogUtil.info('b: invoke expect error');
      try {
        enterpriseBt.turnOffBluetooth({bundleName:'', abilityName:''});
      } catch (err) {
        threw = true;
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('b: ' + JSON.stringify(err));
      }
      LogUtil.info('b: threw=' + threw + ' errCode=' + errCode);
      assertThrownErrIn(threw, errCode, BT_DOC_ERR_CODES);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNOFFBLUETOOTH_R
     * @tc.name ent_turnOffBluetooth_r
     * @tc.desc Verify Bluetooth API ent turnOffBluetooth return value scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOffBluetooth_r', 0, () => {
      let errCode = 0;
      LogUtil.info('r: invoke');
      try {
        enterpriseBt.turnOffBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('r: ' + JSON.stringify(err));
      }
      LogUtil.info('r: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNOFFBLUETOOTH_X
     * @tc.name ent_turnOffBluetooth_x
     * @tc.desc Verify Bluetooth API ent turnOffBluetooth repeat invocation scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOffBluetooth_x', 0, () => {
      let errCode = 0;
      LogUtil.info('x1: invoke');
      try {
        enterpriseBt.turnOffBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x1: ' + JSON.stringify(err));
      }
      LogUtil.info('x1: errCode=' + errCode);
      assertPeerExpect(errCode);
      errCode = 0;
      LogUtil.info('x2: invoke');
      try {
        enterpriseBt.turnOffBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('x2: ' + JSON.stringify(err));
      }
      LogUtil.info('x2: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNOFFBLUETOOTH_C
     * @tc.name ent_turnOffBluetooth_c
     * @tc.desc Verify Bluetooth API ent turnOffBluetooth condition scenario
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOffBluetooth_c', 0, () => {
      let errCode = 0;
      LogUtil.info('c: invoke');
      try {
        enterpriseBt.turnOffBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
      } catch (err) {
        errCode = Number((err as BusinessError).code);
        if (isNaN(errCode)) {
          errCode = 2900099;
        }
        LogUtil.error('c: ' + JSON.stringify(err));
      }
      LogUtil.info('c: errCode=' + errCode);
      assertPeerExpect(errCode);
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNOFFBLUETOOTH_PERF
     * @tc.name ent_turnOffBluetooth_perf
     * @tc.desc Verify Bluetooth apitest case ent turnOffBluetooth perf
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOffBluetooth_perf', 0, () => {
      runPerf('ent_turnOffBluetooth_perf', 5000, () => {
        try {
          enterpriseBt.turnOffBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_ENT_TURNOFFBLUETOOTH_STRESS
     * @tc.name ent_turnOffBluetooth_stress
     * @tc.desc Verify Bluetooth apitest case ent turnOffBluetooth stress
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('ent_turnOffBluetooth_stress', 0, () => {
      runStress('ent_turnOffBluetooth_stress', 20, () => {
        try {
          enterpriseBt.turnOffBluetooth({bundleName:'com.example.test', abilityName:'EntryAbility'});
        } catch (_e) {
          LogUtil.error(JSON.stringify(_e));
        }
      });
    });
    });
  });
}
