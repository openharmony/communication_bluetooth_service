/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Multi-module Scenario suite (1A). Generated by skills/ohtest/bluetooth_scenario_modules.py
 */
import { describe, beforeEach, afterEach, it, expect } from '@ohos/hypium';
import LogUtil from '../../../main/ets/Utils/LogUtil';
import access from '@ohos.bluetooth.access';
import connection from '@ohos.bluetooth.connection';
import ble from '@ohos.bluetooth.ble';
import socket from '@ohos.bluetooth.socket';
import constant from '@ohos.bluetooth.constant';

import {
  assertLocalOk,
  assertPeerExpect,
  ensureBtOn,
  scInvoke,
  scEnable,
  scDisable,
  scGetState,
  scWaitStateOn,
  scAssertPreferOn,
  scAssertStateOn,
  scAssertStateOff,
  scAssertValidState,
  scAssertAfterDisable,
  scWaitStateOff,
  scAwaitStateOn,
  scAwaitStateOff,
  scDismissBtEnableDialog,
  scGetLocalName,
  scSetLocalName,
  scAssertNameRoundTrip,
  scAssertNameCannotSet,
  scAssertNameWhileBtOff,
  scAssertLocalFail,
  scAssertLocalFailOrBtOff,
  scRestoreName,
  scSafeStopBleScan,
  scSafeStopDiscovery,
  scSafeStopAdvertising,
  scSafeCloseGatt,
  scRepeat,
  scProfileIds,
  scScanOpt,
  scFilterEmpty,
  scFilterUuid,
  scAdvertiseSetting,
  scSppOpt,
  scAppendBleFinds,
  scAppendScannerFinds,
  scBleOnFind,
  scBleOffFind,
  scStartBleScan,
  scAmbientScanOpt,
  scClassicOnFind,
  scClassicOffFind,
  scStartClassicDiscovery,
  scPrepareAndStartClassicDiscovery,
  scAppendClassicFinds,
  SC_CLASSIC_FIND_WAIT_MS,
  scScannerOnFind,
  scScannerOffFind,
  scSafeStopScanner,
  scScannerStartScan,
  scScannerStopScan,
  scAssertBleScanHit,
  scAssertAmbientFindHit,
  scAssertBleScanMiss,
  scAssertBleCannotScan,
  SC_BLE_FIND_WAIT_MS,
  SC_BLE_MISS_UUID,
  bleAd,
  bleAs,
  bleAp,
  bleGattService,
  sppOpt,
  legacySppOpt,
  sysBleStartOpt,
  sysBleStopOpt,
  sysBleFoundOpt,
  partnerDeviceAddr,
  SC_SVC_UUID,
  SC_FAKE_MAC,
  BADDR,
  EMPTY_MAC,
  SPP_UUID
  } from './ApitestScenarioShared';

export default function bluetoothScenarioSocket01() {
  describe('bluetoothScenarioSocket01', () => {
    beforeEach(async () => {
      ensureBtOn();
      if (scGetState() !== access.BluetoothState.STATE_ON) {
        scEnable();
        await scDismissBtEnableDialog(2000);
        await new Promise<void>((resolve: () => void) => {
          setTimeout(resolve, 1500);
        });
      }
    });
    afterEach(async () => {
      scSafeStopBleScan();
      scSafeStopDiscovery();
      scSafeStopAdvertising();
      ensureBtOn();
      if (scGetState() !== access.BluetoothState.STATE_ON) {
        scEnable();
        await scDismissBtEnableDialog(2000);
        await new Promise<void>((resolve: () => void) => {
          setTimeout(resolve, 1500);
        });
      }
    });

    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_ENABLE_SPPLISTEN_INSECURE
     * @tc.name sock_enable_sppListen_insecure
     * @tc.desc L1: enable → socket.sppListen(insecure)
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_enable_sppListen_insecure', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      const opt = scSppOpt(false);
      const err = scInvoke(() => {
        socket.sppListen('sc_insecure', opt, () => {
        });
      });
      LogUtil.info('sppListen insecure err=' + err);
      assertLocalOk(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_ENABLE_SPPLISTEN_SECURE
     * @tc.name sock_enable_sppListen_secure
     * @tc.desc L1: enable → socket.sppListen(secure)
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_enable_sppListen_secure', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      const opt = scSppOpt(true);
      const err = scInvoke(() => {
        socket.sppListen('sc_secure', opt, () => {
        });
      });
      LogUtil.info('sppListen secure err=' + err);
      assertLocalOk(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_SPPLISTEN_UUID_VARIANTS
     * @tc.name sock_sppListen_uuid_variants
     * @tc.desc L2: enable → sppListen with sppOpt / custom name
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_sppListen_uuid_variants', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      assertLocalOk(scInvoke(() => {
        socket.sppListen('sc_a', sppOpt(), () => {
        });
      }));
      assertLocalOk(scInvoke(() => {
        socket.sppListen('sc_b', scSppOpt(false), () => {
        });
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_ENABLE_GETDEVICEID
     * @tc.name sock_enable_getDeviceId
     * @tc.desc L1: enable → socket.getDeviceId (no live socket → peer/local)
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_enable_getDeviceId', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      const err = scInvoke(() => {
        socket.getDeviceId(0);
      });
      LogUtil.info('getDeviceId err=' + err);
      assertPeerExpect(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_ENABLE_GETL2CAPPSM
     * @tc.name sock_enable_getL2capPsm
     * @tc.desc L1: enable → socket.getL2capPsm (no live socket → peer/local)
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_enable_getL2capPsm', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      const err = scInvoke(() => {
        socket.getL2capPsm(0);
      });
      LogUtil.info('getL2capPsm err=' + err);
      assertPeerExpect(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_ENABLE_GETMAXRECEIVEDATASIZE
     * @tc.name sock_enable_getMaxReceiveDataSize
     * @tc.desc L1: enable → socket.getMaxReceiveDataSize (no live socket → peer/local)
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_enable_getMaxReceiveDataSize', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      const err = scInvoke(() => {
        socket.getMaxReceiveDataSize(0);
      });
      LogUtil.info('getMaxReceiveDataSize err=' + err);
      assertPeerExpect(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_ENABLE_GETMAXTRANSMITDATASIZE
     * @tc.name sock_enable_getMaxTransmitDataSize
     * @tc.desc L1: enable → socket.getMaxTransmitDataSize (no live socket → peer/local)
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_enable_getMaxTransmitDataSize', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      const err = scInvoke(() => {
        socket.getMaxTransmitDataSize(0);
      });
      LogUtil.info('getMaxTransmitDataSize err=' + err);
      assertPeerExpect(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_ENABLE_ISCONNECTED
     * @tc.name sock_enable_isConnected
     * @tc.desc L1: enable → socket.isConnected (no live socket → peer/local)
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_enable_isConnected', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      const err = scInvoke(() => {
        socket.isConnected(0);
      });
      LogUtil.info('isConnected err=' + err);
      assertPeerExpect(err);
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_ON_OFF_SPPREAD
     * @tc.name sock_on_off_sppRead
     * @tc.desc L2: enable → on(sppRead) → off(sppRead)
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_on_off_sppRead', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      assertLocalOk(scInvoke(() => {
        socket.on('sppRead', 0, () => {
        });
      }));
      assertLocalOk(scInvoke(() => {
        socket.off('sppRead', 0);
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_TRIPLE_LISTEN_ON_OFF
     * @tc.name sock_triple_listen_on_off
     * @tc.desc L2: enable → sppListen → on(sppRead) → off
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_triple_listen_on_off', 0, () => {
      assertLocalOk(scEnable());
      scAssertStateOn(scWaitStateOn());

      assertLocalOk(scInvoke(() => {
        socket.sppListen('sc_t', sppOpt(), () => {
        });
      }));
      assertLocalOk(scInvoke(() => {
        socket.on('sppRead', 0, () => {
        });
      }));
      assertLocalOk(scInvoke(() => {
        socket.off('sppRead', 0);
      }));
    });


    /**
     * @tc.number SUB_BLUETOOTHTEST_APITEST_SCENARIO_SOCK_DISABLE_SPPLISTEN
     * @tc.name sock_disable_sppListen
     * @tc.desc L1: disable → sppListen
     * @tc.size MediumTest
     * @tc.type Function
     * @tc.level Level 2
     */
    it('sock_disable_sppListen', 0, async () => {
      assertLocalOk(scDisable());
      scAssertStateOff(await scAwaitStateOff(2500));
      assertLocalOk(scInvoke(() => {
        socket.sppListen('sc_off', sppOpt(), () => {
        });
      }));
      }, 45000);

  });
}
